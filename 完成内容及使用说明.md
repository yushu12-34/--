# PostgreSQL 性能优化 — 完成内容及使用说明

## 一、优化目标

将 API 读取路径从「整库快照加载（readJson）」改造为「PostgreSQL 专用 SQL 查询」，避免全表读取，提升并发性能和响应速度。

## 二、完成内容

### 2.1 新增 12 个 PostgreSQL 专用只读视图方法

文件：`apps/api/src/services/postgresStore.js`

| 方法名 | 对应路由 | 说明 |
|--------|----------|------|
| `listProjectsView(userId)` | `GET /api/projects` | 项目列表，含画布/资产/历史聚合统计 |
| `getProjectView(projectId, userId)` | `GET /api/projects/:id` | 项目详情，含画布列表和成员 |
| `getCanvasView(canvasId)` | `GET /api/canvases/:id` | 单画布信息 |
| `listCanvasMembersView(canvasId)` | `GET /api/canvases/:id/members` | 画布成员列表 |
| `listAssetsView(projectId)` | `GET /api/assets` | 资产列表（按项目过滤） |
| `getTaskView(taskId)` | `GET /api/ai/tasks/:id` | AI 任务详情 |
| `getAdminOverviewView()` | `GET /internal/overview` | 后台概览统计 |
| `listAdminTasksView(limit)` | `GET /internal/tasks` | 后台任务列表 |
| `getAdminTaskView(taskId)` | `GET /internal/tasks/:id` | 后台任务详情 |
| `listSystemEventsView(filters)` | `GET /internal/system-events` | 系统事件列表 |
| `listYjsHistoryView(canvasId)` | `GET /api/canvases/:id/yjs-history` | Yjs 历史快照列表 |
| `getYjsHistorySnapshotView(canvasId, snapshotId)` | `GET /api/canvases/:id/yjs-history/:sid` | Yjs 历史快照详情 |

### 2.2 db.js 层暴露后端判断和视图方法

文件：`apps/api/src/db.js`

- 新增 `usePostgresBackend()` — 供 server.js 判断当前是否使用 PostgreSQL 后端
- 重新导出 12 个视图方法，供 server.js 路由直接调用

### 2.3 server.js 路由分流改造

文件：`apps/api/src/server.js`

改造策略：
1. 在 `handle()` 函数顶部调用 `usePostgresBackend()` 判断后端类型
2. 对已优化的 GET 路由，优先使用 PostgreSQL 专用查询方法，**不调用 `readJson()`**
3. 未命中专用路径的请求，回退到原有的 `readJson()` 整库快照逻辑

已分流的 GET 路由（PostgreSQL 模式下不再加载整库快照）：
- `GET /api/projects`
- `GET /api/projects/:id`
- `GET /api/canvases/:id`
- `GET /api/canvases/:id/members`
- `GET /api/assets`
- `GET /api/ai/tasks/:id`
- `GET /internal/overview`
- `GET /internal/tasks`
- `GET /internal/tasks/:id`
- `GET /internal/system-events`
- `GET /api/canvases/:id/yjs-history`
- `GET /api/canvases/:id/yjs-history/:sid`

### 2.4 语法验证

所有修改文件通过 `node --check` 语法检查，无语法错误。

## 三、使用说明

### 3.1 前置条件

- PostgreSQL 容器已启动并健康运行
- 已安装 `pg` npm 包（`npm --prefix apps/api install pg`）

### 3.2 环境变量配置

启动 API 服务前，需设置以下环境变量：

```bash
# 必须设置为 postgres 才启用 PostgreSQL 后端
export DATA_BACKEND=postgres

# 数据库连接字符串（与 postgres-service/.env 中的保持一致）
export DATABASE_URL=postgresql://anime_canvas_app:yushu123@127.0.0.1:5433/anime_canvas

# 用户指纹密钥（生产环境务必替换为随机 32+ 字节字符串）
export USER_FINGERPRINT_SECRET=replace-with-at-least-32-random-bytes

# 内部管理接口令牌（生产环境务必替换）
export INTERNAL_ADMIN_TOKEN=replace-with-strong-random-token
```

### 3.3 启动 PostgreSQL 容器

```bash
cd /ssd_data/PostgreSQL/postgres-service
docker compose up -d
./scripts/check.sh   # 验证容器健康和表结构
```

### 3.4 启动 API 服务

```bash
cd /ssd_data/PostgreSQL/apps/api

# 方式一：直接设置环境变量启动
DATA_BACKEND=postgres \
DATABASE_URL=postgresql://anime_canvas_app:yushu123@127.0.0.1:5433/anime_canvas \
USER_FINGERPRINT_SECRET=your-secret-key \
node src/server.js

# 方式二：开发模式（自动重启）
DATA_BACKEND=postgres \
DATABASE_URL=postgresql://anime_canvas_app:yushu123@127.0.0.1:5433/anime_canvas \
USER_FINGERPRINT_SECRET=your-secret-key \
node --watch src/server.js
```

### 3.5 验证优化生效

```bash
# 1. 检查项目列表（应返回数据，不再加载整库快照）
curl -s http://localhost:8787/api/projects | head -c 200

# 2. 检查健康接口
curl -s http://localhost:8787/api/health

# 3. 观察 API 日志，确认无 "readJson" 整库加载日志
```

### 3.6 暂停/恢复 PostgreSQL 容器

```bash
cd /ssd_data/PostgreSQL/postgres-service

# 暂停（保留容器和数据）
docker compose stop

# 恢复
docker compose up -d
```

> 注意：这些命令仅影响当前 `postgres-service` 目录下的容器，不会影响其他项目的 Docker 容器。

## 四、回退方案

如需回退到 JSON 文件存储模式：

```bash
# 取消 DATA_BACKEND 环境变量或设为 json
unset DATA_BACKEND
# 或
export DATA_BACKEND=json

# 重启 API 服务
node src/server.js
```

此时所有路由将回退到原有的 `readJson()` 整库快照逻辑。

## 五、注意事项

1. **`/api/models` 路由**：models 列表需要关联 providers 数据，暂未实现专用查询方法，PostgreSQL 模式下仍走 `readJson()` 回退路径。后续可新增 `listModelsView()` 优化。
2. **写操作**：所有 POST/PUT/PATCH/DELETE 请求仍走原有逻辑（通过 `updateJson()` 写入），PostgreSQL 写入路径已在之前阶段实现。
3. **生产环境密钥**：`USER_FINGERPRINT_SECRET` 和 `INTERNAL_ADMIN_TOKEN` 必须替换为强随机值，不要使用默认占位符。

