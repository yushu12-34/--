# 发布前运维说明

本文档用于内测、预发和小规模上线前的本地运维检查。客户创作端、开发者管理端和后端 API 已分离，发布前请优先确认端口、接口边界、数据备份和预检结果。

## 1. 本地启动

首次安装依赖：

```powershell
cd E:\画板
npm run setup
```

启动全部本地服务：

```powershell
npm run dev
```

默认访问地址：

| 服务 | 地址 | 用途 |
| --- | --- | --- |
| 客户创作端 | `http://localhost:5180` | 客户使用的画布、项目、素材、历史和公开模型参数 |
| 开发者管理端 | `http://localhost:5190` | 开发人员/运维人员配置供应商、模型、密钥、内部参数和任务诊断 |
| API 健康检查 | `http://localhost:8787/api/health` | 后端公开健康检查 |

停止全部本地服务：

```powershell
npm run stop
```

## 2. 发布前预检

启动服务后运行：

```powershell
npm run preflight
```

预检会串联以下检查：

- Node.js 运行时是否可用。
- `apps/api/node_modules` 和 `apps/web/node_modules` 是否存在。
- API、客户创作端、开发者管理端是否能返回 2xx。
- `npm run benchmark:canvas` 大画布性能基准是否通过。
- `npm run check` 后端语法检查、自动化测试、Web/Admin 构建是否通过。
- `npm run security:preflight` 内部接口安全边界是否通过。
- `npm run smoke:release` 发布候选冒烟验收是否通过。
- `npm run recovery:drill` 数据备份与恢复演练是否通过。

如果只需要执行完整测试和构建，可单独运行：

```powershell
npm run check
```

单独运行接口安全检查：

```powershell
npm run security:preflight
```

单独运行发布候选冒烟验收：

```powershell
npm run smoke:release
```

冒烟验收会验证 API/Web/Admin 探活、公开模型契约、Admin 内部只读接口、任务富化字段、运行日志筛选、客户 `/admin` 路由隔离，并用临时 token API 实例确认未授权内部访问会写入 `security/warning` 事件。

单独运行数据恢复演练：

```powershell
npm run recovery:drill
```

恢复演练会启动独立临时 `DATA_DIR` 和随机端口 API，验证项目导入前备份、历史恢复前备份、备份失败时 503 阻断写入、备份失败事件记录，以及手动将备份文件恢复为 `db.json` 后重启 API 仍能读回旧快照。默认会自动删除临时数据；需要保留演练目录时可直接运行 `powershell -ExecutionPolicy Bypass -File scripts/recovery-drill.ps1 -KeepTempData`。

发布前要求内部接口必须配置 token 时，运行严格检查：

```powershell
npm run security:release
```

安全检查会验证：

- `/api/models` 只返回客户可见字段，不包含供应商、密钥、adapter、请求模板、内部参数 schema 和字段映射。
- `/internal/*` 与兼容的 `/api/admin/*` 在配置 `INTERNAL_ADMIN_TOKEN` 后拒绝未授权请求，并接受正确 token。
- 客户创作端源码和构建产物不包含 `/internal`、`/api/admin`、`VITE_INTERNAL_ADMIN_TOKEN` 等内部入口或 token 引用。

## 3. 入口与接口边界

客户创作端只面向最终客户：

- 只访问 `/api/*` 公开接口。
- 只展示公开模型名称、能力、`defaultPublicParams` 和 `publicParamSchema`。
- 访问 `/admin` 时只显示不可用入口，不渲染开发者后台。
- 不接收供应商 `baseUrl`、密钥、adapter、请求模板、轮询模板、字段映射和任务内部诊断。

开发者管理端只面向开发人员和运维人员：

- 访问 `/internal/*` 内部接口。
- 可配置供应商、模型、内部执行参数、客户公开参数、连接测试和任务诊断。
- 配置 `INTERNAL_ADMIN_TOKEN` 后，后台请求需要携带 token；前端管理端同步配置 `VITE_INTERNAL_ADMIN_TOKEN`。
- 建议只部署在本机、内网或受访问控制保护的环境中。

## 4. 环境变量

参考 `.env.example`：

```text
API_PORT=8787
WEB_PORT=5180
ADMIN_PORT=5190
DATA_DIR=data
VITE_API_BASE_URL=http://localhost:8787/api
VITE_INTERNAL_API_BASE_URL=http://localhost:8787/internal
INTERNAL_ADMIN_TOKEN=
VITE_INTERNAL_ADMIN_TOKEN=
Z_IMAGE_BASE_URL=http://localhost:8192
Z_IMAGE_REMOTE_BASE_URL=http://192.168.100.100:8191
```

发布或内测前重点确认：

- `VITE_API_BASE_URL` 只能指向公开 API。
- `VITE_INTERNAL_API_BASE_URL` 只用于开发者管理端。
- `INTERNAL_ADMIN_TOKEN` 不应暴露给客户创作端。
- 供应商密钥当前仍是开发环境 `plain-local-json` 存储，不应作为正式生产密钥管理方案。

## 5. 数据与备份

默认数据目录：

```text
data/
  db.json
  backups/
```

当前后端仍使用本地 JSON 存储，尚未迁移到 PostgreSQL。项目导入和历史恢复属于危险写入，写入前会自动创建 `data/backups` 备份；如果备份失败，后端会返回 503 并阻断写入。

发布前建议手动确认：

- `DATA_DIR` 指向预期目录。
- `data/db.json` 存在且可读写。
- `data/backups` 可写入。
- 最近一次项目导入或历史恢复后，备份文件数量有增长。
- `npm run recovery:drill` 通过，确认备份失败阻断和手动恢复流程仍可用。

## 6. 常见故障处理

端口被占用：

```powershell
npm run stop
npm run dev
```

预检提示服务不可达：

- 先确认 `npm run dev` 已启动。
- 检查 `.local/logs/api.err.log`、`.local/logs/web.err.log`、`.local/logs/admin.err.log`。
- 确认 `.env` 中的 `API_PORT`、`WEB_PORT`、`ADMIN_PORT` 与访问地址一致。

Web/Admin 构建失败：

```powershell
npm run check
```

根据输出定位 TypeScript、测试或构建错误。

模型任务失败：

- 打开开发者管理端 `http://localhost:5190`。
- 在任务列表中查看模型、供应商、耗时、输入摘要和错误分类。
- 根据错误分类处理连接失败、鉴权失败、超时、非 JSON、字段映射或模型返回错误。

运行日志检查：

- 打开开发者管理端 `http://localhost:5190`，进入“运行日志”。
- 重点查看 `错误` 与 `警告` 数量、最近错误时间，以及 `安全`、`任务`、`模型`、`备份` 分类。
- 日志最多保留最近 500 条，元数据会自动脱敏 token、secret、authorization、apiKey 等字段。
- 可直接调用内部接口 `GET /internal/system-events?limit=100` 查看原始事件数据。

## 7. 发布前 Checklist

- `npm run preflight` 全部通过。
- `npm run security:release` 在配置 `INTERNAL_ADMIN_TOKEN` 后通过。
- 客户创作端 `http://localhost:5180` 可打开画布，状态栏无新错误。
- 客户创作端访问 `/admin` 不进入开发者后台。
- 开发者管理端 `http://localhost:5190` 可打开任务列表和任务详情。
- 开发者管理端“运行日志”可打开，能看到 API 启动、安全拦截、模型测试、任务失败或备份失败等事件。
- `/api/models` 不返回 `baseUrl`、`secretValue`、adapter、请求模板、轮询模板和字段映射。
- `/internal/*` 在配置 token 后能拒绝未授权请求。
- `data/db.json` 和 `data/backups` 路径确认无误。
- `.env` 中的内部 token、供应商密钥和模型服务地址未写入客户可见配置。
