# 无限画布 AI 动漫创作工具开发计划

## 1. 项目定位

本项目目标是开发一个基于无限画布的 AI 动漫创作工具。用户可以在画布中拖拽不同类型的节点，通过连线组合文本、图片、视频、音频等素材，并调用 AI 模型生成动漫图片、音频和视频内容。

产品形态类似工作流节点编辑器，同时结合动漫创作流程，支持从角色设定、分镜、图片生成、配音、视频生成到最终导出的完整创作链路。

核心方向：

- 无限画布：支持节点自由拖拽、缩放、平移、连线和组合。
- 节点工作流：使用统一节点协议管理输入节点、生成节点、处理节点和输出节点。
- AI 多模态生成：支持文生图、图生图、文生音频、文生视频、多模态视频生成。
- 多人协作：采用 Yjs + WebSocket，实现多人实时编辑和节点占用状态显示。
- 模型后台管理：提供后台管理页面配置模型服务、接口地址、密钥、参数模板和启停状态。
- WebUI：提供创作端 WebUI 和管理端 WebUI。

---

## 2. 推荐总体方案

### 2.1 前端方案

推荐使用：

- React 18
- TypeScript
- Vite
- WeiUI
- React Flow / XYFlow
- Zustand
- TanStack Query
- Yjs
- y-websocket client

前端负责：

- 无限画布交互。
- 节点展示与编辑。
- 节点连线和连接校验。
- AI 任务发起、进度展示和结果预览。
- 多人协作状态展示。
- 创作端 WebUI。
- 管理端 WebUI。

第一阶段不建议自研完整画布引擎，优先基于 React Flow / XYFlow 快速实现节点画布。后续当节点数量、渲染性能或交互复杂度达到瓶颈时，再考虑自研 Canvas/WebGL 混合画布引擎。

### 2.2 后端方案

推荐使用：

- Node.js
- NestJS 或 Fastify
- PostgreSQL
- Redis
- BullMQ
- WebSocket
- Yjs WebSocket 服务
- MinIO / S3 兼容对象存储

后端负责：

- 用户、项目、画布、素材、模型配置管理。
- AI 任务创建、排队、执行、取消、重试。
- 模型供应商适配。
- 文件上传与媒体存储。
- WebSocket 推送任务进度。
- Yjs 协作同步服务。
- 管理后台接口。

### 2.3 协作方案

明确选择推荐方案：

```text
Yjs + WebSocket
```

使用 Yjs 管理多人协作数据，使用 WebSocket 传输增量更新。

协作数据分两类：

1. 持久化协作文档数据：节点、连线、节点数据、画布元信息。
2. 非持久化在线感知数据：在线用户、鼠标位置、选中节点、正在编辑的节点、正在拖拽的节点。

---

## 3. 系统模块划分

## 3.1 创作端 WebUI

创作端是普通用户使用的主要界面。

页面结构：

```text
顶部栏：项目名称 / 保存 / 撤销 / 重做 / 运行 / 导出 / 协作状态
左侧栏：节点组件库
中间：无限画布
右侧栏：节点属性面板 / 模型参数 / 结果预览
底部栏：任务队列 / 生成进度 / 在线用户 / 缩放比例
```

核心功能：

- 创建项目。
- 打开画布。
- 拖拽节点到画布。
- 节点编辑。
- 节点连接。
- 连接规则校验。
- 单节点运行。
- 结果预览。
- 素材入库。
- 多人在线状态显示。

## 3.2 管理端 WebUI

管理端用于配置模型、任务队列、供应商、系统参数和用户权限。

建议后台路径：

```text
/admin
```

管理端主要页面：

| 页面 | 功能 |
| --- | --- |
| 仪表盘 | 查看任务数量、成功率、失败率、平均耗时、模型调用量 |
| 模型供应商管理 | 配置 ComfyUI、Stable Diffusion、TTS、视频模型等供应商 |
| 模型列表管理 | 配置具体模型名称、类型、能力、默认参数和启停状态 |
| 模型参数模板 | 配置图片尺寸、采样步数、CFG、音色、视频时长等参数模板 |
| API 密钥管理 | 配置第三方模型服务密钥，密钥加密保存，不在前端明文展示 |
| 任务队列管理 | 查看运行中、排队中、失败、完成的 AI 任务 |
| 素材管理 | 查看和清理用户生成的图片、音频、视频 |
| 用户与权限 | 管理用户、团队、角色和访问权限 |
| 系统设置 | 配置存储、并发数、文件大小限制、回调地址等 |

---

## 4. 节点系统设计

## 4.1 节点分类

现阶段基础节点：

| 分类 | 节点 | 说明 |
| --- | --- | --- |
| 输入节点 | 文本节点 | 输入提示词文本，输出 text |
| 输入节点 | 图片节点 | 上传或选择图片，输出 image |
| 输入节点 | 视频节点 | 上传或选择视频，输出 video |
| 输入节点 | 音频节点 | 上传或选择音频，输出 audio |
| 生成节点 | 图片生成节点 | 接收 text 和可选 image，输出 image |
| 生成节点 | 音频生成节点 | 接收 text，输出 audio |
| 生成节点 | 视频生成节点 | 接收 text，可选 image、video、audio，输出 video |

后续扩展节点：

- 角色设定节点。
- 世界观设定节点。
- 分镜脚本节点。
- 镜头运动节点。
- 表情控制节点。
- 姿势控制节点。
- 角色一致性节点。
- 图片放大节点。
- 图片修复节点。
- 视频补帧节点。
- 字幕节点。
- 导出节点。

## 4.2 节点协议

```typescript
type MediaType = 'text' | 'image' | 'video' | 'audio';

type NodeRuntimeStatus =
  | 'idle'
  | 'pending'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'cancelled';

interface WorkflowNode {
  id: string;
  type: string;
  title: string;
  position: {
    x: number;
    y: number;
  };
  data: Record<string, unknown>;
  runtime: {
    status: NodeRuntimeStatus;
    taskId?: string;
    progress?: number;
    error?: string;
  };
  createdAt: string;
  updatedAt: string;
}

interface NodeDefinition {
  type: string;
  name: string;
  category: 'input' | 'generate' | 'utility' | 'output';
  description?: string;
  inputs: PortDefinition[];
  outputs: PortDefinition[];
  defaultData: Record<string, unknown>;
  configurable?: boolean;
}

interface PortDefinition {
  id: string;
  name: string;
  direction: 'input' | 'output';
  mediaType: MediaType;
  required?: boolean;
  multiple?: boolean;
  maxConnections?: number;
}

interface WorkflowEdge {
  id: string;
  sourceNodeId: string;
  sourcePortId: string;
  targetNodeId: string;
  targetPortId: string;
}
```

## 4.3 连接规则

基础规则：

1. output 端口只能连接 input 端口。
2. 输入和输出的 mediaType 必须一致。
3. required 端口必须有连接，否则节点不能运行。
4. multiple=false 的 input 端口只能连接一个来源。
5. 不允许节点连接自身。
6. 默认不允许形成循环依赖。
7. 删除节点时自动删除相关连线。
8. 删除连线后重新校验目标节点状态。

图片生成节点规则：

```text
必须连接 text。
可以连接多个 image 作为参考图。
输出 image。
```

音频生成节点规则：

```text
必须连接 text。
输出 audio。
```

视频生成节点规则：

```text
必须连接 text。
可以连接 image、video、audio 作为参考。
输出 video。
```

---

## 5. AI 模型配置后台设计

## 5.1 模型供应商配置

后台需要支持多个模型供应商。

示例供应商类型：

| 类型 | 说明 |
| --- | --- |
| comfyui | ComfyUI 工作流服务 |
| sd-webui | Stable Diffusion WebUI |
| openai-compatible | OpenAI 兼容接口 |
| tts | TTS 语音生成服务 |
| video-provider | 视频生成服务 |
| custom-http | 自定义 HTTP 模型接口 |

供应商配置结构：

```typescript
interface ModelProviderConfig {
  id: string;
  name: string;
  type: 'comfyui' | 'sd-webui' | 'openai-compatible' | 'tts' | 'video-provider' | 'custom-http';
  baseUrl: string;
  authType: 'none' | 'api-key' | 'bearer' | 'basic';
  encryptedSecret?: string;
  timeoutSeconds: number;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
}
```

## 5.2 模型列表配置

每一个模型都归属于一个供应商。

```typescript
interface AIModelConfig {
  id: string;
  providerId: string;
  name: string;
  displayName: string;
  type: 'image' | 'audio' | 'video' | 'text';
  capabilities: Array<'text-to-image' | 'image-to-image' | 'text-to-speech' | 'text-to-video' | 'image-to-video' | 'multimodal-video'>;
  defaultParams: Record<string, unknown>;
  paramSchema: Record<string, unknown>;
  enabled: boolean;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
}
```

后台模型配置能力：

- 新增模型。
- 编辑模型。
- 启用/禁用模型。
- 测试连接。
- 设置默认参数。
- 设置参数表单 Schema。
- 设置模型可见范围。
- 查看模型调用统计。

## 5.3 参数模板

图片生成参数示例：

```json
{
  "width": 1024,
  "height": 1024,
  "steps": 28,
  "cfgScale": 7,
  "seed": -1,
  "sampler": "DPM++ 2M Karras",
  "style": "anime"
}
```

音频生成参数示例：

```json
{
  "voice": "default_female",
  "speed": 1,
  "pitch": 1,
  "emotion": "neutral"
}
```

视频生成参数示例：

```json
{
  "duration": 5,
  "fps": 24,
  "width": 1280,
  "height": 720,
  "motionStrength": 0.6,
  "cameraMotion": "none"
}
```

## 5.4 密钥安全

密钥必须由后端加密保存，前端管理端只能显示脱敏结果。

要求：

- API Key 不允许返回明文。
- 日志中不打印密钥。
- 测试连接只返回成功、失败和错误摘要。
- 支持更换密钥。
- 支持禁用供应商。

---

## 6. AI 任务系统设计

## 6.1 任务流程

```text
用户点击生成
  ↓
前端校验节点连接规则
  ↓
前端收集上游节点输入
  ↓
调用后端创建 AI 任务
  ↓
后端根据节点类型选择模型配置
  ↓
任务写入 BullMQ 队列
  ↓
Worker 调用对应模型供应商
  ↓
生成过程中推送进度
  ↓
生成完成后上传结果到对象存储
  ↓
保存素材记录
  ↓
更新节点 runtime 和 data
  ↓
通过 WebSocket / Yjs 同步给协作者
```

## 6.2 任务状态

```typescript
type AITaskStatus =
  | 'pending'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'cancelled';

interface AITask {
  id: string;
  projectId: string;
  canvasId: string;
  nodeId: string;
  modelId: string;
  type: 'image.generate' | 'audio.generate' | 'video.generate';
  status: AITaskStatus;
  input: Record<string, unknown>;
  output?: Record<string, unknown>;
  error?: string;
  progress?: number;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}
```

## 6.3 队列策略

现阶段建议：

- 图片生成队列、音频生成队列、视频生成队列分开。
- 视频任务单独限制并发。
- 每个用户设置最大并发任务数。
- 每个项目设置最大排队任务数。
- 失败任务支持重试。
- 用户可以取消任务。

---

## 7. Yjs + WebSocket 协作设计

## 7.1 协作文档结构

每个画布对应一个 Yjs 文档。

文档 ID 建议：

```text
canvas:{canvasId}
```

Yjs 数据结构：

```typescript
interface YCanvasDocumentShape {
  nodes: Y.Map<WorkflowNode>;
  edges: Y.Array<WorkflowEdge>;
  meta: Y.Map<unknown>;
}
```

建议同步内容：

- 节点新增。
- 节点删除。
- 节点位置变更。
- 节点 data 变更。
- 节点 runtime 状态。
- 连线新增。
- 连线删除。
- 画布标题。

不建议同步内容：

- 用户本地缩放比例。
- 用户本地选区。
- 用户右侧面板打开状态。

这些属于本地 UI 状态或 presence 状态。

## 7.2 Presence 在线感知

通过 Yjs awareness 或自定义 WebSocket presence 管理。

```typescript
interface UserPresence {
  userId: string;
  nickname: string;
  color: string;
  avatarUrl?: string;
  cursor?: {
    x: number;
    y: number;
  };
  selectedNodeIds?: string[];
  editingNodeId?: string;
  draggingNodeId?: string;
  lastActiveAt: string;
}
```

创作端展示：

- 在线用户列表。
- 鼠标光标。
- 节点上的用户头像。
- 正在编辑提示。
- 正在拖拽提示。

## 7.3 节点占用方案

现阶段采用软锁：

```text
用户 A 正在编辑节点时，节点上显示用户 A。
其他用户仍可编辑，但界面提示“此节点正在被 A 编辑”。
```

后续升级为硬锁：

```text
用户 A 获得节点编辑锁后，其他用户只能查看。
锁超时、断线或主动关闭时自动释放。
```

## 7.4 数据持久化

现阶段方案：

- 前端通过 Yjs 同步实时协作。
- 后端定期将 Yjs 文档快照保存到 PostgreSQL。
- 关键操作后触发增量保存。
- 项目关闭或用户离开时触发保存。

后续升级：

- 增加操作日志。
- 增加版本历史。
- 支持回滚到历史版本。
- 支持分支和副本。

---

## 8. 数据库核心表设计

建议核心表：

```text
users
projects
canvases
assets
model_providers
ai_models
model_param_templates
ai_tasks
workflow_snapshots
```

## 8.1 projects

```typescript
interface ProjectRecord {
  id: string;
  name: string;
  ownerId: string;
  coverAssetId?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 8.2 canvases

```typescript
interface CanvasRecord {
  id: string;
  projectId: string;
  name: string;
  snapshotId?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 8.3 assets

```typescript
interface AssetRecord {
  id: string;
  projectId: string;
  type: 'image' | 'audio' | 'video';
  url: string;
  thumbnailUrl?: string;
  mimeType: string;
  size: number;
  width?: number;
  height?: number;
  duration?: number;
  source: 'upload' | 'ai-generated';
  createdBy: string;
  createdAt: string;
}
```

---

## 9. 现阶段分阶段开发与测试计划

## 阶段 1：基础项目与画布原型

目标：实现可拖拽、可连线、可编辑的基础画布。

开发内容：

- 初始化前端项目。
- 接入 WeiUI。
- 接入 React Flow / XYFlow。
- 创建主画布页面。
- 创建左侧节点库。
- 创建右侧属性面板。
- 实现文本节点。
- 实现图片节点。
- 实现图片生成节点的前端 UI。
- 实现基础连线。
- 实现端口类型校验。
- 实现本地状态保存与恢复。

测试内容：

- 节点能正常创建、拖拽、删除。
- 节点位置保存后能恢复。
- 文本节点能编辑 prompt。
- 图片节点能上传并预览图片。
- 图片生成节点能识别已连接的文本和图片。
- 错误类型的端口不能连接。
- 缺少必填 text 输入时，图片生成按钮不可用。

阶段验收标准：

```text
用户可以在画布中创建文本节点、图片节点、图片生成节点，并通过合法连线组成图片生成流程。
```

## 阶段 2：后端基础服务与数据持久化

目标：实现项目、画布、素材和基础 API。

开发内容：

- 初始化后端 API 服务。
- 建立用户、项目、画布、素材表。
- 实现项目创建、查询、更新、删除接口。
- 实现画布保存和加载接口。
- 实现素材上传接口。
- 接入 MinIO 或 S3 兼容存储。
- 前端接入真实保存接口。

测试内容：

- 创建项目后可再次打开。
- 画布节点和连线保存后可恢复。
- 图片上传后可在节点中预览。
- 素材记录能正确入库。
- 大文件上传失败时有错误提示。

阶段验收标准：

```text
用户创建的项目、画布、节点、连线和素材可以持久化保存，并能重新加载。
```

## 阶段 3：模型配置后台与图片生成链路

目标：实现模型后台管理，并跑通第一条 AI 生成链路。

开发内容：

- 创建管理端 WebUI。
- 实现模型供应商管理。
- 实现模型列表管理。
- 实现模型参数模板管理。
- 实现 API 密钥加密保存。
- 实现模型连接测试。
- 实现 AI 任务创建接口。
- 接入 BullMQ 和 Redis。
- 实现图片生成 Worker。
- 支持 ComfyUI 或 Stable Diffusion WebUI 中至少一种图片模型。
- 生成结果上传对象存储。
- 生成结果回写图片生成节点。

测试内容：

- 管理员可以新增供应商。
- 管理员可以新增图片模型。
- 管理员可以启用/禁用模型。
- 模型连接测试能返回成功或失败。
- 文本节点连接图片生成节点后可以发起生成任务。
- 生成进度能展示。
- 生成成功后图片显示在节点中。
- 生成失败后节点显示错误信息。
- 密钥不会在接口和日志中明文出现。

阶段验收标准：

```text
用户可以通过 文本节点 → 图片生成节点 生成图片，生成结果进入节点和素材库。
```

## 阶段 4：Yjs + WebSocket 多人协作 MVP

目标：实现多人同时编辑同一个画布。

开发内容：

- 搭建 Yjs WebSocket 服务。
- 每个画布创建独立 Yjs 文档。
- 同步 nodes、edges、meta。
- 使用 awareness 同步在线用户。
- 显示在线用户列表。
- 显示用户光标。
- 显示节点正在编辑者。
- 实现软锁提示。
- 定期保存 Yjs 快照到数据库。

测试内容：

- 两个浏览器打开同一画布，新增节点能实时同步。
- 拖拽节点时另一个浏览器能看到位置变化。
- 修改文本节点内容能同步。
- 新增和删除连线能同步。
- 在线用户列表能正确显示。
- 用户编辑节点时，节点上能显示该用户。
- 用户断开后在线状态能移除。
- Yjs 快照能保存和恢复。

阶段验收标准：

```text
多人可以同时进入一个画布，并实时看到节点、连线和编辑状态变化。
```

## 阶段 5：音频生成和视频生成

目标：增加多模态生成能力。

开发内容：

- 实现音频输入节点。
- 实现视频输入节点。
- 实现音频生成节点。
- 实现视频生成节点。
- 后台增加音频模型配置。
- 后台增加视频模型配置。
- Worker 增加音频任务队列。
- Worker 增加视频任务队列。
- 视频任务单独限制并发。
- 生成结果加入素材库。

测试内容：

- 文本节点连接音频生成节点后能生成音频。
- 音频结果可以播放。
- 文本节点连接视频生成节点后能生成视频。
- 图片、音频、视频参考输入能被视频生成节点识别。
- 视频任务执行中能展示进度。
- 视频任务失败时能重试或取消。

阶段验收标准：

```text
系统支持图片、音频、视频三类 AI 生成任务，并能在画布和素材库中管理结果。
```

## 阶段 6：工作流运行引擎

目标：从单节点运行升级为流程运行。

开发内容：

- 构建 DAG 执行图。
- 检测循环依赖。
- 实现拓扑排序。
- 实现运行当前节点。
- 实现运行下游节点。
- 实现运行整个工作流。
- 实现任务缓存。
- 实现失败中断和局部重试。

测试内容：

- 有向无环图可以正确排序运行。
- 缺少必填输入时工作流不能运行。
- 有循环依赖时给出错误提示。
- 上游节点完成后下游节点自动运行。
- 某个节点失败后下游节点不会继续错误运行。

阶段验收标准：

```text
用户可以一键运行当前工作流，并按依赖顺序自动完成多节点生成。
```

## 阶段 7：优化、权限与上线准备

目标：提升系统稳定性、性能、安全性和可运营能力。

开发内容：

- 增加权限系统。
- 增加团队空间。
- 增加项目分享。
- 增加版本历史。
- 增加画布性能优化。
- 增加任务统计。
- 增加错误监控。
- 增加部署脚本。
- 增加自动化测试。

测试内容：

- 权限隔离正确。
- 非项目成员不能访问项目。
- 大量节点下画布仍可操作。
- 高并发任务不会导致队列异常。
- 后端异常时前端有明确提示。
- 部署后服务可稳定运行。

阶段验收标准：

```text
系统达到可内测或小规模上线状态。
```

---

## 10. 后续升级路线

## 10.1 画布引擎升级路线

现阶段：

```text
React Flow / XYFlow
```

适合快速开发 MVP。

后续选择：

| 路线 | 适用场景 | 说明 |
| --- | --- | --- |
| 继续深度定制 React Flow | 节点数量中等，业务优先 | 维护成本低，迭代快 |
| DOM + Canvas 混合渲染 | 连线很多、节点 UI 复杂 | 节点用 DOM，连线用 Canvas |
| WebGL 画布引擎 | 上千节点、复杂动效 | 性能强，但开发成本高 |
| 自研画布引擎 | 产品交互高度特殊 | 控制力最强，但周期最长 |

推荐升级顺序：

```text
React Flow → DOM + Canvas 混合 → WebGL / 自研引擎
```

## 10.2 协作系统升级路线

现阶段：

```text
Yjs + WebSocket + awareness + 定时快照
```

后续升级：

- 增加服务端 Yjs 文档持久化。
- 增加操作日志。
- 增加版本历史。
- 增加历史回滚。
- 增加硬锁。
- 增加评论和批注。
- 增加团队权限。
- 增加多人语音/会议协作入口。

## 10.3 AI 模型系统升级路线

现阶段：

```text
后台手动配置模型供应商和模型参数
```

后续升级：

- 支持多供应商自动路由。
- 支持失败自动切换备用模型。
- 支持按成本、速度、质量选择模型。
- 支持模型调用统计和成本统计。
- 支持用户级模型权限。
- 支持团队级模型额度。
- 支持插件化模型适配器。
- 支持 ComfyUI 工作流模板导入。
- 支持用户自定义模型节点。

## 10.4 节点系统升级路线

现阶段：

```text
内置固定节点
```

后续升级：

- 插件化节点注册。
- 自定义节点参数表单。
- 节点模板市场。
- 工作流模板市场。
- 动漫角色一致性节点。
- 分镜脚本节点。
- 字幕节点。
- 导出成片节点。
- 批量生成节点。

## 10.5 后端架构升级路线

现阶段：

```text
单体 API + Worker + Redis + PostgreSQL + 对象存储
```

后续升级：

- API 服务拆分。
- Worker 横向扩容。
- 视频任务独立服务。
- 文件转码独立服务。
- 任务事件总线。
- 多租户隔离。
- Kubernetes 部署。
- CDN 加速素材访问。

---

## 11. 推荐优先级

第一优先级：

```text
画布 MVP + 文本节点 + 图片节点 + 图片生成节点 + 图片生成链路
```

第二优先级：

```text
项目保存 + 素材库 + 模型后台配置
```

第三优先级：

```text
Yjs + WebSocket 多人协作
```

第四优先级：

```text
音频生成 + 视频生成
```

第五优先级：

```text
工作流执行引擎 + 插件化节点 + 性能优化
```

---

## 12. 最小可用版本 MVP 定义

MVP 只做最小闭环：

```text
创建项目
  ↓
进入无限画布
  ↓
添加文本节点
  ↓
输入动漫提示词
  ↓
添加图片生成节点
  ↓
连接文本节点和图片生成节点
  ↓
点击生成
  ↓
后端调用图片模型
  ↓
生成图片
  ↓
图片显示在节点中
  ↓
生成结果保存到素材库
```

MVP 不做：

- 完整多人协作。
- 视频生成。
- 音频生成。
- 硬锁。
- 插件市场。
- 复杂权限。
- 自研画布引擎。
- 工作流全自动运行。

---

## 13. 开发建议总结

建议先用成熟方案快速跑通产品核心闭环。

现阶段推荐路线：

```text
React + TypeScript + WeiUI + React Flow
Node.js + PostgreSQL + Redis + BullMQ
Yjs + WebSocket
MinIO / S3
后台模型管理 WebUI
```

先完成单人可用的 AI 图片生成工作流，再接入模型后台和素材库，然后再做 Yjs 多人协作。这样风险最低，开发节奏最稳。

后续再逐步扩展音频、视频、工作流自动运行、硬锁、模板市场、插件系统和 WebGL 级别画布性能优化。
