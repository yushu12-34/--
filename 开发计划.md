# 无限画布 AI 动漫创作工具开发计划

## 1. 项目定位

本项目目标是开发一个基于无限画布的 AI 动漫创作工具。用户可以在画布中拖拽不同类型的节点，通过连线组合文本、图片、视频、音频等素材，并调用 AI 模型生成动漫图片、音频和视频内容。

产品形态类似工作流节点编辑器，同时结合动漫创作流程，支持从角色设定、分镜、图片生成、配音、视频生成到最终导出的完整创作链路。

核心方向：

- 无限画布：支持节点自由拖拽、缩放、平移、连线和组合。
- 节点工作流：使用统一节点协议管理输入节点、生成节点、处理节点和输出节点。
- AI 多模态生成：支持文生图、图生图、文生音频、文生视频、多模态视频生成。
- 多人协作：采用 Yjs + WebSocket，实现多人实时编辑和节点占用状态显示。
- 模型后台管理：提供后台管理页面配置模型服务、接口地址、密钥、参数模板和启停状态。
- WebUI：提供客户创作端 WebUI 和开发者/运维管理端 WebUI，两者在阶段 7A 分离。

---

## 当前开发进度（已更新）

当前进入 **阶段 8：生产部署与内测上线**，7C 稳定性系列已全部完成，PostgreSQL 迁移已正式执行。

已完成：

- 阶段 1：基础项目与画布原型。
- 阶段 2：后端基础服务与数据持久化。
- 阶段 3：模型配置后台与图片生成链路。
- 阶段 3B：Redis/BullMQ 队列、MinIO 存储、Z-Image / 自定义 HTTP 模型适配器。
- 阶段 4：多人协作 MVP。
- 阶段 4C：协作稳定性与画布基础操作优化。
- 阶段 5A：素材库完整模块。
- 阶段 5B：节点运行体验优化。
- 阶段 5C：画布使用体验优化。
- 阶段 6A：组合框内工作流运行。
- 阶段 6B：节点缓存与输入变更检测。
- 阶段 6C-1：当前协作 MVP 收口。
- 阶段 6C-2A：前端 Yjs 文档适配层与单人数据源桥接。
- 阶段 6C-2B：细粒度节点/连线/组合操作迁移到 Y.Doc。
- 阶段 6C-2C：文本与历史增强。
- 阶段 6C-3A：服务端 Yjs update relay 与前端接入。
- 阶段 6C-3B：sync/state-vector 差量握手。
- 阶段 6C-3C：awareness 兼容桥。
- 阶段 6C-4：Yjs 持久化与历史基础。
- 阶段 6D-1：项目管理与导入导出入口。
- 阶段 6D-2：历史版本详情与回滚。
- 阶段 6D-3：项目列表页与历史体验打磨。
- 阶段 7A：客户创作端与开发者模型配置后台分离。
- 阶段 7B：模型参数可视化与自定义 HTTP 适配器增强（含任务诊断增强：模型/供应商/耗时/输入摘要/错误分类/重试入口）。
- 阶段 7C-1A 至 7C-1V：画布代码拆分、数据备份、性能基准与优化、生产包体拆分、预发检查脚本、发布前运维说明、内部接口安全预检、运行日志面板、发布候选冒烟验收、数据恢复演练、素材库刷新状态对账、连接空白处快速补节点、后端断线恢复与连线释放兜底、PostgreSQL 存储适配层代码接入、JSON 到 PostgreSQL 迁移脚本与导入后验收。

当前能力：

- 多用户、项目、画布基础管理。
- 无限画布节点创建、连接、拖拽、分组、撤销/重做。
- 框选后自动生成组合背景，不再需要选区确认。
- 组合背景内可运行组合工作流。
- 生成节点支持排队中、运行中、成功、失败、缓存命中等状态。
- 节点结果支持保存素材、复制 URL、预览、使用缓存运行、强制重新生成。
- 素材库支持筛选、预览、重命名、删除、复制 URL、拖拽到画布；刷新后素材数量会跟随当前项目摘要和项目绑定加载结果，不再被旧请求覆盖为 0。
- 协作支持在线用户、光标、编辑状态、断线重连、快照同步。
- 协作支持正在编辑节点展示、远端快照来源识别、本地编辑保护和冲突提示。
- 前端已具备 Yjs 文档适配层，支持 CanvasSnapshot 与 Y.Doc 双向转换、本地 UI 状态桥接和 Y.UndoManager 撤销/重做入口。
- 节点新增、删除、复制、拖拽，连线新增、删除，组合创建、解除、重命名、拖拽和运行状态已写入 Y.Doc 细粒度结构。
- 文本节点和图片生成节点的 `data.prompt` 已升级为 `Y.Text`，并支持输入时实时写入 Y.Doc 与撤销/重做恢复。
- 协作 WebSocket 已支持 `yjs:state-vector` / `yjs:sync` / `yjs:update`，服务端维护每个 canvasId 的 Y.Doc room，并支持客户端进入房间后的双向差量合并。
- 协作在线用户、光标、选中节点和正在编辑节点已接入 awareness 兼容桥，当前 UI 保持不变，底层可逐步迁移到标准 awareness protocol。
- 服务端已保存 Yjs update log，支持压缩为 Yjs snapshot，并可在服务重启后按 canvasId 恢复协作文档。
- 顶栏已接入项目切换、新建、重命名、复制、删除、JSON 导入/导出入口。
- 项目 JSON 导出已包含项目、画布、素材、Yjs update log 和 Yjs snapshot；导入/复制时会重映射画布 ID，避免历史记录串到原画布。
- 项目抽屉已集中展示项目、画布数量、素材数量、历史快照数量和更新时间，并支持直接切换、导出、复制和删除。
- 画布已具备 Yjs 历史快照面板，可查看快照列表并手动压缩当前 Yjs 文档。
- Yjs 历史快照支持详情摘要，能查看节点、连线、组合数量，并可确认恢复到选中历史版本。
- 恢复历史版本前会自动保存当前版本，并展示当前/目标版本摘要差异；空快照会额外确认，恢复失败会在历史面板提示。
- 导入项目 JSON 已增加版本和结构兼容校验，避免无效文件写入项目数据。
- 恢复后写入当前 Y.Doc 与 CanvasSnapshot 兼容层，并通过协作通道通知其他浏览器。
- 节点输入签名支持缓存命中跳过和结果过期提示。

下一阶段建议：

- ~~阶段 7A：客户创作端与开发者模型配置后台分离~~（已完成）
- ~~阶段 7B：模型参数可视化、自定义 HTTP 适配器增强与内测稳定性收口~~（已完成）
- ~~阶段 7C：内测稳定性、权限与上线准备~~（已完成）
- ~~阶段 7C-1T：PostgreSQL 部署与用户数据隔离设计~~（已完成）
- ~~阶段 7C-1U：PostgreSQL 存储适配层代码接入~~（已完成首版）
- ~~阶段 7C-1V：JSON 到 PostgreSQL 迁移脚本与导入后验收~~（已完成）
- 阶段 8：生产部署与内测上线——服务器部署、前端静态文件部署、生产环境配置、权限系统 MVP、项目成员分享

---

## 2. 推荐总体方案

### 2.1 前端方案

推荐使用：

- React 18
- TypeScript
- Vite
- WeiUI
- React Flow / XYFlow
- Zustand
- TanStack Query
- Yjs
- y-websocket client

前端负责：

- 无限画布交互。
- 节点展示与编辑。
- 节点连线和连接校验。
- AI 任务发起、进度展示和结果预览。
- 多人协作状态展示。
- 创作端 WebUI。
- 管理端 WebUI。

第一阶段不建议自研完整画布引擎，优先基于 React Flow / XYFlow 快速实现节点画布。后续当节点数量、渲染性能或交互复杂度达到瓶颈时，再考虑自研 Canvas/WebGL 混合画布引擎。

### 2.2 后端方案

推荐使用：

- Node.js
- NestJS 或 Fastify
- PostgreSQL
- Redis
- BullMQ
- WebSocket
- Yjs WebSocket 服务
- MinIO / S3 兼容对象存储

后端负责：

- 用户、项目、画布、素材、模型配置管理。
- AI 任务创建、排队、执行、取消、重试。
- 模型供应商适配。
- 文件上传与媒体存储。
- WebSocket 推送任务进度。
- Yjs 协作同步服务。
- 管理后台接口。

### 2.3 协作方案

明确选择推荐方案：

```text
Yjs + WebSocket
```

使用 Yjs 管理多人协作数据，使用 WebSocket 传输增量更新。

协作数据分两类：

1. 持久化协作文档数据：节点、连线、节点数据、画布元信息。
2. 非持久化在线感知数据：在线用户、鼠标位置、选中节点、正在编辑的节点、正在拖拽的节点。

---

## 3. 系统模块划分

## 3.1 创作端 WebUI

创作端是普通用户使用的主要界面。

页面结构：

```text
顶部栏：项目名称 / 保存 / 撤销 / 重做 / 运行 / 导出 / 协作状态
左侧栏：节点组件库
中间：无限画布
右侧栏：节点属性面板 / 模型参数 / 结果预览
底部栏：任务队列 / 生成进度 / 在线用户 / 缩放比例
```

核心功能：

- 创建项目。
- 打开画布。
- 拖拽节点到画布。
- 节点编辑。
- 节点连接。
- 连接规则校验。
- 单节点运行。
- 结果预览。
- 素材入库。
- 多人在线状态显示。

## 3.2 开发者/运维管理端 WebUI

开发者/运维管理端用于配置模型、任务队列、供应商、系统参数和用户权限，不作为客户前端入口。

当前路径规划：

```text
阶段 7A 前：apps/web 临时挂载 /admin
阶段 7A 后：apps/admin 独立应用，建议默认端口 5190
```

开发者/运维管理端主要页面：

| 页面 | 功能 |
| --- | --- |
| 仪表盘 | 查看任务数量、成功率、失败率、平均耗时、模型调用量 |
| 模型供应商管理 | 配置 ComfyUI、Stable Diffusion、TTS、视频模型等供应商 |
| 模型列表管理 | 配置具体模型名称、类型、能力、默认参数和启停状态 |
| 模型参数模板 | 配置图片尺寸、采样步数、CFG、音色、视频时长等参数模板 |
| API 密钥管理 | 配置第三方模型服务密钥，密钥加密保存，不在前端明文展示 |
| 任务队列管理 | 查看运行中、排队中、失败、完成的 AI 任务 |
| 素材管理 | 查看和清理用户生成的图片、音频、视频 |
| 用户与权限 | 管理用户、团队、角色和访问权限 |
| 系统设置 | 配置存储、并发数、文件大小限制、回调地址等 |

---

## 4. 节点系统设计

## 4.1 节点分类

现阶段基础节点：

| 分类 | 节点 | 说明 |
| --- | --- | --- |
| 输入节点 | 文本节点 | 输入提示词文本，输出 text |
| 输入节点 | 图片节点 | 上传或选择图片，输出 image |
| 输入节点 | 视频节点 | 上传或选择视频，输出 video |
| 输入节点 | 音频节点 | 上传或选择音频，输出 audio |
| 生成节点 | 图片生成节点 | 接收 text 和可选 image，输出 image |
| 生成节点 | 音频生成节点 | 接收 text，输出 audio |
| 生成节点 | 视频生成节点 | 接收 text，可选 image、video、audio，输出 video |

后续扩展节点：

- 角色设定节点。
- 世界观设定节点。
- 分镜脚本节点。
- 镜头运动节点。
- 表情控制节点。
- 姿势控制节点。
- 角色一致性节点。
- 图片放大节点。
- 图片修复节点。
- 视频补帧节点。
- 字幕节点。
- 导出节点。

## 4.2 节点协议

```typescript
type MediaType = 'text' | 'image' | 'video' | 'audio';

type NodeRuntimeStatus =
  | 'idle'
  | 'pending'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'cancelled';

interface WorkflowNode {
  id: string;
  type: string;
  title: string;
  position: {
    x: number;
    y: number;
  };
  data: Record<string, unknown>;
  runtime: {
    status: NodeRuntimeStatus;
    taskId?: string;
    progress?: number;
    error?: string;
  };
  createdAt: string;
  updatedAt: string;
}

interface NodeDefinition {
  type: string;
  name: string;
  category: 'input' | 'generate' | 'utility' | 'output';
  description?: string;
  inputs: PortDefinition[];
  outputs: PortDefinition[];
  defaultData: Record<string, unknown>;
  configurable?: boolean;
}

interface PortDefinition {
  id: string;
  name: string;
  direction: 'input' | 'output';
  mediaType: MediaType;
  required?: boolean;
  multiple?: boolean;
  maxConnections?: number;
}

interface WorkflowEdge {
  id: string;
  sourceNodeId: string;
  sourcePortId: string;
  targetNodeId: string;
  targetPortId: string;
}
```

## 4.3 连接规则

基础规则：

1. output 端口只能连接 input 端口。
2. 输入和输出的 mediaType 必须一致。
3. required 端口必须有连接，否则节点不能运行。
4. multiple=false 的 input 端口只能连接一个来源。
5. 不允许节点连接自身。
6. 默认不允许形成循环依赖。
7. 删除节点时自动删除相关连线。
8. 删除连线后重新校验目标节点状态。

图片生成节点规则：

```text
必须连接 text。
可以连接多个 image 作为参考图。
输出 image。
```

音频生成节点规则：

```text
必须连接 text。
输出 audio。
```

视频生成节点规则：

```text
必须连接 text。
可以连接 image、video、audio 作为参考。
输出 video。
```

---

## 5. AI 模型配置后台设计

## 5.1 模型供应商配置

后台需要支持多个模型供应商。

示例供应商类型：

| 类型 | 说明 |
| --- | --- |
| comfyui | ComfyUI 工作流服务 |
| sd-webui | Stable Diffusion WebUI |
| openai-compatible | OpenAI 兼容接口 |
| tts | TTS 语音生成服务 |
| video-provider | 视频生成服务 |
| custom-http | 自定义 HTTP 模型接口 |

供应商配置结构：

```typescript
interface ModelProviderConfig {
  id: string;
  name: string;
  type: 'comfyui' | 'sd-webui' | 'openai-compatible' | 'tts' | 'video-provider' | 'custom-http';
  baseUrl: string;
  authType: 'none' | 'api-key' | 'bearer' | 'basic';
  encryptedSecret?: string;
  timeoutSeconds: number;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
}
```

## 5.2 模型列表配置

每一个模型都归属于一个供应商。

```typescript
interface AIModelConfig {
  id: string;
  providerId: string;
  name: string;
  displayName: string;
  type: 'image' | 'audio' | 'video' | 'text';
  capabilities: Array<'text-to-image' | 'image-to-image' | 'text-to-speech' | 'text-to-video' | 'image-to-video' | 'multimodal-video'>;
  defaultParams: Record<string, unknown>;
  paramSchema: Record<string, unknown>;
  enabled: boolean;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
}
```

后台模型配置能力：

- 新增模型。
- 编辑模型。
- 启用/禁用模型。
- 测试连接。
- 设置默认参数。
- 设置参数表单 Schema。
- 设置模型可见范围。
- 查看模型调用统计。

## 5.3 参数模板

图片生成参数示例：

```json
{
  "width": 1024,
  "height": 1024,
  "steps": 28,
  "cfgScale": 7,
  "seed": -1,
  "sampler": "DPM++ 2M Karras",
  "style": "anime"
}
```

音频生成参数示例：

```json
{
  "voice": "default_female",
  "speed": 1,
  "pitch": 1,
  "emotion": "neutral"
}
```

视频生成参数示例：

```json
{
  "duration": 5,
  "fps": 24,
  "width": 1280,
  "height": 720,
  "motionStrength": 0.6,
  "cameraMotion": "none"
}
```

## 5.4 密钥安全

密钥必须由后端加密保存，前端管理端只能显示脱敏结果。

要求：

- API Key 不允许返回明文。
- 日志中不打印密钥。
- 测试连接只返回成功、失败和错误摘要。
- 支持更换密钥。
- 支持禁用供应商。

---

## 6. AI 任务系统设计

## 6.1 任务流程

```text
用户点击生成
  ↓
前端校验节点连接规则
  ↓
前端收集上游节点输入
  ↓
调用后端创建 AI 任务
  ↓
后端根据节点类型选择模型配置
  ↓
任务写入 BullMQ 队列
  ↓
Worker 调用对应模型供应商
  ↓
生成过程中推送进度
  ↓
生成完成后上传结果到对象存储
  ↓
保存素材记录
  ↓
更新节点 runtime 和 data
  ↓
通过 WebSocket / Yjs 同步给协作者
```

## 6.2 任务状态

```typescript
type AITaskStatus =
  | 'pending'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'cancelled';

interface AITask {
  id: string;
  projectId: string;
  canvasId: string;
  nodeId: string;
  modelId: string;
  type: 'image.generate' | 'audio.generate' | 'video.generate';
  status: AITaskStatus;
  input: Record<string, unknown>;
  output?: Record<string, unknown>;
  error?: string;
  progress?: number;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}
```

## 6.3 队列策略

现阶段建议：

- 图片生成队列、音频生成队列、视频生成队列分开。
- 视频任务单独限制并发。
- 每个用户设置最大并发任务数。
- 每个项目设置最大排队任务数。
- 失败任务支持重试。
- 用户可以取消任务。

---

## 7. Yjs + WebSocket 协作设计

## 7.1 协作文档结构

每个画布对应一个 Yjs 文档。

当前实现边界：

```text
自定义 WebSocket presence + 画布快照广播
```

当前实现已经能同步在线用户、光标、编辑状态和画布快照，但不是完整 CRDT 增量同步。远端更新仍以整份 snapshot 覆盖本地状态，后续需要升级为真正的 Yjs 文档模型，避免多人同时编辑时互相覆盖。

文档 ID 建议：

```text
canvas:{canvasId}
```

Yjs 数据结构：

```typescript
interface YCanvasDocumentShape {
  nodes: Y.Map<Y.Map<unknown>>;
  nodeOrder: Y.Array<string>;
  edges: Y.Map<Y.Map<unknown>>;
  edgeOrder: Y.Array<string>;
  groups: Y.Map<Y.Map<unknown>>;
  groupOrder: Y.Array<string>;
  meta: Y.Map<unknown>;
}
```

建议将节点、连线、组合从数组迁移为 `Y.Map + order array`：

- `nodes` 按节点 ID 存储节点内容，便于多人同时编辑不同节点。
- `nodeOrder` 存储显示顺序，避免对象 Map 无序导致 UI 抖动。
- `edges` 按连线 ID 存储连线内容，新增/删除连线可独立同步。
- `groups` 存储组合框信息，包括成员节点、边界、运行状态。
- `meta` 存储画布名称、版本号、最后保存时间等共享元信息。

节点内部建议进一步拆分：

```typescript
interface YWorkflowNodeShape {
  id: string;
  type: string;
  title: string;
  position: Y.Map<number>;
  data: Y.Map<unknown>;
  runtime: Y.Map<unknown>;
  createdAt: string;
  updatedAt: string;
}
```

其中 `text.input.data.prompt` 可升级为 `Y.Text`，支持多人同时编辑长文本；其他结构化参数继续使用 `Y.Map`。

建议同步内容：

- 节点新增。
- 节点删除。
- 节点位置变更。
- 节点 data 变更。
- 节点 runtime 状态。
- 连线新增。
- 连线删除。
- 画布标题。

不建议同步内容：

- 用户本地缩放比例。
- 用户本地选区。
- 用户右侧面板打开状态。

这些属于本地 UI 状态或 presence 状态。

## 7.2 Presence 在线感知

通过 Yjs awareness 或自定义 WebSocket presence 管理。

```typescript
interface UserPresence {
  userId: string;
  nickname: string;
  color: string;
  avatarUrl?: string;
  cursor?: {
    x: number;
    y: number;
  };
  selectedNodeIds?: string[];
  editingNodeId?: string;
  draggingNodeId?: string;
  lastActiveAt: string;
}
```

创作端展示：

- 在线用户列表。
- 鼠标光标。
- 节点上的用户头像。
- 正在编辑提示。
- 正在拖拽提示。

## 7.3 节点占用方案

现阶段采用软锁：

```text
用户 A 正在编辑节点时，节点上显示用户 A。
其他用户仍可编辑，但界面提示“此节点正在被 A 编辑”。
```

后续升级为硬锁：

```text
用户 A 获得节点编辑锁后，其他用户只能查看。
锁超时、断线或主动关闭时自动释放。
```

## 7.4 数据持久化

现阶段方案：

- 前端通过自定义 WebSocket 广播画布快照。
- 后端通过 `/api/canvases/:id/snapshot` 保存最新快照到本地 JSON。
- 关键操作后触发自动保存。
- API 不可用时回退到浏览器本地存储。

完整 Yjs CRDT 目标方案：

- 前端使用 Yjs document 作为画布真实数据源。
- 后端维护每个 canvasId 对应的 Y.Doc，并通过 WebSocket 传输 Yjs update。
- 后端保存 Yjs 二进制 update log，并定期压缩成 snapshot。
- API 加载画布时优先恢复 Yjs snapshot，再 replay 增量 update。
- 导出项目时将 Yjs 文档转换为当前 CanvasSnapshot JSON。

后续升级：

- 增加操作日志。
- 增加版本历史。
- 支持回滚到历史版本。
- 支持分支和副本。

### 7.5 完整 Yjs CRDT 落地计划

#### 7.5.1 数据源迁移

目标：让前端画布状态从 React 本地数组迁移为 Yjs 文档派生状态。

开发内容：

- 新增 `apps/web/src/yCanvasDocument.ts`，封装 Yjs 文档读写。
- 定义 `encodeWorkflowNodeToYMap` / `decodeWorkflowNodeFromYMap`。
- 定义 `applySnapshotToYDoc` / `snapshotFromYDoc`，兼容现有 `CanvasSnapshot`。
- CanvasPage 初始化时：先加载后端 snapshot，再写入 Y.Doc。
- React Flow 的 nodes/edges 由 Y.Doc 观察器派生，不再由本地 setState 作为唯一事实源。
- 本地 undo/redo 迁移到 `Y.UndoManager`，先覆盖节点 data、position、edges、groups。

验收标准：

```text
单人模式下，所有节点新增、删除、移动、编辑、连线、组合操作都通过 Y.Doc 更新，并能保存/恢复为现有快照格式。
```

#### 7.5.2 服务端 Yjs WebSocket

目标：将现有 snapshot 广播升级为 Yjs update 增量同步。

开发内容：

- 安装并接入 `yjs`、`y-protocols`。
- 扩展 `collaborationService.js`，支持 sync protocol 与 awareness protocol。
- 每个 canvasId 在服务端维护一个 Y.Doc room。
- 客户端连接后先同步服务端 Y.Doc，再进入正常 update 广播。
- 保留旧 `snapshot:update` 协议作为迁移兼容，直到前端完全切换。
- 增加心跳和 room 空闲释放策略，避免长期占用内存。

验收标准：

```text
两个浏览器同时打开同一画布，新增节点、拖拽节点、编辑 prompt、连线和组合操作都以增量同步，不再整份 snapshot 覆盖。
```

#### 7.5.3 持久化与历史版本

目标：让协作文档可恢复、可压缩、可回滚。

开发内容：

- 新增 `workflow_updates` 存储概念：canvasId、clock、update、createdAt。
- 本地 JSON 阶段可将 update 以 base64 数组保存；PostgreSQL 阶段改为 bytea。
- 定期合并 update，生成 `workflow_snapshots`。
- 支持按时间点恢复：snapshot + 截止到目标时间的 update replay。
- 画布复制时复制当前 Y.Doc 快照，而不是只复制旧 JSON snapshot。
- 导入/导出继续使用可读 JSON，导入时转换为 Y.Doc。

验收标准：

```text
服务重启后能恢复最近协作文档；用户可查看历史快照并回滚到指定版本。
```

#### 7.5.4 冲突提示与软锁

目标：在 CRDT 自动合并基础上，为用户提供可理解的协作状态。

开发内容：

- 使用 Yjs awareness 管理在线用户、光标、选中节点、editingNodeId、draggingNodeId。
- 节点卡片展示远端编辑者、拖拽者和最近更新时间。
- 本地正在编辑字段收到远端 update 时，显示冲突提示，不直接打断输入焦点。
- 对 prompt 使用 Y.Text 后，提供多人文本编辑光标或至少显示正在编辑者。
- 软锁继续允许编辑，但对高风险字段提示“其他用户正在编辑”。
- 后续硬锁可通过 `locks` Y.Map 实现，带 owner、expiresAt、reason。

验收标准：

```text
多人同时编辑同一节点时不会丢失内容，界面能显示谁在编辑，并在本地编辑被远端变更影响时给出明确提示。
```

#### 7.5.5 测试计划

自动化测试：

- Y.Doc 与 CanvasSnapshot 双向转换不丢字段。
- 并发新增不同节点后最终文档包含两个节点。
- 并发移动同一节点后最终状态一致且不损坏节点 data。
- 并发新增/删除连线后不会出现悬空连线。
- update log replay 后结果与原文档一致。

手动验收：

- 双浏览器同时编辑同一画布。
- 断网重连后自动同步最新状态。
- 服务重启后恢复画布。
- 大画布频繁拖拽时同步不卡顿。
- 历史版本回滚后两端都看到同一结果。

---

## 8. 数据库核心表设计

建议核心表：

```text
users
projects
canvases
assets
model_providers
ai_models
model_param_templates
ai_tasks
workflow_snapshots
```

## 8.1 projects

```typescript
interface ProjectRecord {
  id: string;
  name: string;
  ownerId: string;
  coverAssetId?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 8.2 canvases

```typescript
interface CanvasRecord {
  id: string;
  projectId: string;
  name: string;
  snapshotId?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 8.3 assets

```typescript
interface AssetRecord {
  id: string;
  projectId: string;
  type: 'image' | 'audio' | 'video';
  url: string;
  thumbnailUrl?: string;
  mimeType: string;
  size: number;
  width?: number;
  height?: number;
  duration?: number;
  source: 'upload' | 'ai-generated';
  createdBy: string;
  createdAt: string;
}
```

---

## 9. 现阶段分阶段开发与测试计划

## 阶段 1：基础项目与画布原型

目标：实现可拖拽、可连线、可编辑的基础画布。

开发内容：

- 初始化前端项目。
- 接入 WeiUI。
- 接入 React Flow / XYFlow。
- 创建主画布页面。
- 创建左侧节点库。
- 创建右侧属性面板。
- 实现文本节点。
- 实现图片节点。
- 实现图片生成节点的前端 UI。
- 实现基础连线。
- 实现端口类型校验。
- 实现本地状态保存与恢复。

测试内容：

- 节点能正常创建、拖拽、删除。
- 节点位置保存后能恢复。
- 文本节点能编辑 prompt。
- 图片节点能上传并预览图片。
- 图片生成节点能识别已连接的文本和图片。
- 错误类型的端口不能连接。
- 缺少必填 text 输入时，图片生成按钮不可用。

阶段验收标准：

```text
用户可以在画布中创建文本节点、图片节点、图片生成节点，并通过合法连线组成图片生成流程。
```

## 阶段 2：后端基础服务与数据持久化

目标：实现项目、画布、素材和基础 API。

开发内容：

- 初始化后端 API 服务。
- 建立用户、项目、画布、素材表。
- 实现项目创建、查询、更新、删除接口。
- 实现画布保存和加载接口。
- 实现素材上传接口。
- 接入 MinIO 或 S3 兼容存储。
- 前端接入真实保存接口。

测试内容：

- 创建项目后可再次打开。
- 画布节点和连线保存后可恢复。
- 图片上传后可在节点中预览。
- 素材记录能正确入库。
- 大文件上传失败时有错误提示。

阶段验收标准：

```text
用户创建的项目、画布、节点、连线和素材可以持久化保存，并能重新加载。
```

## 阶段 3：模型配置后台与图片生成链路

目标：实现模型后台管理，并跑通第一条 AI 生成链路。

开发内容：

- 创建管理端 WebUI。
- 实现模型供应商管理。
- 实现模型列表管理。
- 实现模型参数模板管理。
- 实现 API 密钥加密保存。
- 实现模型连接测试。
- 实现 AI 任务创建接口。
- 接入 BullMQ 和 Redis。
- 实现图片生成 Worker。
- 支持 Z-Image 或自定义 HTTP 图片模型。
- 生成结果上传对象存储。
- 生成结果回写图片生成节点。

测试内容：

- 管理员可以新增供应商。
- 管理员可以新增图片模型。
- 管理员可以启用/禁用模型。
- 模型连接测试能返回成功或失败。
- 文本节点连接图片生成节点后可以发起生成任务。
- 生成进度能展示。
- 生成成功后图片显示在节点中。
- 生成失败后节点显示错误信息。
- 密钥不会在接口和日志中明文出现。

阶段验收标准：

```text
用户可以通过 文本节点 → 图片生成节点 生成图片，生成结果进入节点和素材库。
```

## 阶段 3B：完善任务队列与真实模型接入

目标：从模拟实现升级到真实生产级架构。

开发内容：

- 安装 Redis 客户端依赖。
- 安装 BullMQ 依赖。
- 配置 Redis 连接。
- 创建图片生成队列处理器。
- 创建音频生成队列处理器。
- 创建视频生成队列处理器。
- 实现任务优先级。
- 实现队列并发限制。
- 安装 MinIO SDK。
- 配置 MinIO 连接。
- 实现文件上传服务。
- 实现素材存储服务。
- 接入 Z-Image / 自定义 HTTP 模型适配器。
- 实现 Z-Image 文生图接口。
- 实现通用异步任务轮询接口。
- 实现模型参数映射。
- 保留插件化模型适配器扩展点。

测试内容：

- 任务能正确进入队列。
- 队列 Worker 能正确处理任务。
- 多个任务能并行执行（受并发限制）。
- 任务失败能自动重试。
- 任务超时能正确处理。
- 生成结果能正确上传到 MinIO。
- 素材记录能正确关联存储路径。
- Z-Image 文生图能正确返回图片。
- 自定义 HTTP 异步任务能正确轮询并处理结果。
- 模型参数能正确传递。

阶段验收标准：

```text
系统使用真实队列和存储服务，能通过 Z-Image 或自定义 HTTP 模型生成真实图片。
```

## 阶段 4：Yjs + WebSocket 多人协作 MVP（已完成，当前实现为自定义 WebSocket presence + 快照同步）

目标：实现多人同时编辑同一个画布。

开发内容：

- 安装 yjs、y-websocket、y-protocols 依赖。
- 搭建 Yjs WebSocket 服务器。
- 创建 Yjs 文档管理服务。
- 实现画布文档创建与加载。
- 配置 awareness 同步在线用户状态。
- 实现节点变更同步（新增、删除、移动、属性修改）。
- 实现连线变更同步。
- 实现画布元信息同步。
- 前端接入 Yjs Provider。
- 实现在线用户列表组件。
- 实现用户光标显示组件。
- 实现节点编辑者显示。
- 实现节点软锁提示。
- 实现 Yjs 文档定期快照。
- 实现快照持久化到数据库。
- 实现快照恢复功能。

测试内容：

- 两个浏览器打开同一画布，新增节点能实时同步。
- 拖拽节点时另一个浏览器能看到位置变化。
- 修改文本节点内容能同步。
- 新增和删除连线能同步。
- 在线用户列表能正确显示。
- 用户编辑节点时，节点上能显示该用户。
- 用户断开后在线状态能移除。
- Yjs 快照能保存和恢复。

阶段验收标准：

```text
多人可以同时进入一个画布，并实时看到节点、连线和编辑状态变化。
```

## 阶段 5：音频生成和视频生成（部分完成：节点、上传、预览、素材管理已具备，真实音视频模型适配待完善）

目标：增加多模态生成能力。

开发内容：

- 创建音频输入节点组件。
- 创建视频输入节点组件。
- 创建音频生成节点组件。
- 创建视频生成节点组件。
- 实现音频文件上传处理。
- 实现视频文件上传处理。
- 实现音频预览播放功能。
- 实现视频预览播放功能。
- 后台增加音频模型配置页面。
- 后台增加视频模型配置页面。
- 实现 TTS 模型适配器。
- 实现视频生成模型适配器。
- Worker 增加音频任务队列处理器。
- Worker 增加视频任务队列处理器。
- 视频任务单独限制并发数。
- 实现多输入节点数据收集。
- 生成结果自动加入素材库。

测试内容：

- 文本节点连接音频生成节点后能生成音频。
- 音频结果可以在节点中播放。
- 文本节点连接视频生成节点后能生成视频。
- 图片、音频、视频参考输入能被视频生成节点识别。
- 视频任务执行中能展示进度。
- 视频任务失败时能重试或取消。
- 音频文件上传后能预览。
- 视频文件上传后能预览。
- 多输入节点连接时数据能正确收集。

阶段验收标准：

```text
系统支持图片、音频、视频三类 AI 生成任务，并能在画布和素材库中管理结果。
```

## 阶段 6：工作流运行引擎（进行中，已完成 6A / 6B）

目标：从单节点运行升级为组合框内工作流运行，并支持缓存复用。

### 阶段 6A：组合框内工作流运行（已完成）

开发内容：

- 框选多个节点后自动生成组合背景。
- 移除选区确认 UI，不再需要点击确认创建组合。
- 优化组合背景设计，使其更像工作流容器。
- 组合背景上提供操作按钮：运行、重命名、解除组合。
- 只运行组合内的生成节点，不运行整个画布。
- 组合内按 DAG 拓扑顺序运行。
- 输入节点不执行，但可作为组合内或组合外输入被读取。
- 生成节点失败后，其下游依赖节点跳过，其他无依赖分支继续运行。
- 组合背景显示运行中、已完成、失败、跳过数量等状态。

### 阶段 6B：节点缓存与输入变更检测（已完成）

开发内容：

- 为生成节点计算输入签名。
- 输入签名包含上游输入、prompt、参考图、modelId、params。
- 输入未变化且已有结果时，运行节点直接命中缓存。
- 组合工作流运行时自动复用缓存结果。
- 节点结果操作支持“使用缓存运行”和“强制重新生成”。
- 输入变化后节点显示“结果可能已过期”。
- 节点 runtime 增加 inputSignature 和 cacheHit 状态。

### 阶段 6C：协作冲突与软锁增强（进行中，已完成 6C-3A）

目标：先补齐当前 WebSocket 快照协作的冲突提示，再为完整 Yjs CRDT 改造打基础。

#### 阶段 6C-1：当前协作 MVP 收口（已完成）

开发内容：

- 增强节点软锁展示。
- 用户正在编辑节点时，其他用户看到更明确的提示。
- 远端更新覆盖本地正在编辑内容时显示冲突提示。
- 重连后主动拉取最新画布快照。
- 在线用户列表显示每个用户正在编辑的节点。
- 给 snapshot 广播增加版本来源和本地脏字段判断。
- 本地正在编辑 prompt / 参数时，远端 snapshot 不直接覆盖输入框内容，先进入冲突提示状态。
- 在线协作者列表支持显示每个用户正在编辑的节点。
- 新浏览器加入协作房间时，服务端下发房间最新快照。
- 已在线客户端收到快照请求后主动广播当前非空画布。
- 服务端防止新加入客户端的空快照覆盖房间内已有非空快照。
- 修复新浏览器本地为空时因本地版本号较新而拒收远端非空快照的问题。
- 修复编辑状态为空时 presence 无法明确清空的问题。
- 增加协作冲突提示横幅，方便双浏览器验收。

验收标准：

```text
在现有快照同步模式下，多人同时操作不会无提示覆盖本地正在编辑的节点内容。
```

#### 阶段 6C-2：前端 Yjs 文档数据源（进行中）

开发内容：

##### 阶段 6C-2A：Yjs 文档适配层与单人数据源桥接（已完成）

- 安装 `yjs`、`y-protocols`、`y-websocket` 依赖。
- 新增 `apps/web/src/yCanvasDocument.ts`。
- 支持 `CanvasSnapshot ↔ Y.Doc` 双向转换。
- 使用 `Y.Map + order array` 保存 nodes、edges、groups，避免顺序丢失。
- 支持 Yjs update 编码和应用，为服务端增量同步预留入口。
- CanvasPage 加载后端快照、本地快照、远端 snapshot 时写入 Y.Doc。
- React UI 状态变化同步回 Y.Doc，当前阶段保留 React Flow 状态作为 UI 派生层。
- 撤销/重做优先使用 `Y.UndoManager`，本地历史栈作为兼容兜底。
- 增加 `tests/yCanvasDocument.test.mjs`，覆盖快照转换、update 应用、undo/redo。

##### 阶段 6C-2B：细粒度操作迁移到 Y.Doc（已完成）

- 将节点新增、删除、复制、移动改为直接写入 Y.Doc。
- 将连线新增、删除改为直接写入 Y.Doc。
- 将组合创建、解除、重命名、移动、运行状态改为直接写入 Y.Doc。
- 将 `patchNode` 改为更新节点对应的 Y.Map，而不是整份 snapshot 替换。
- 增加 `patchYCanvasNode`、`upsertYCanvasNode(s)`、`removeYCanvasNode`。
- 增加 `upsertYCanvasEdge(s)`、`removeYCanvasEdge`。
- 增加 `patchYCanvasGroup`、`upsertYCanvasGroup(s)`、`removeYCanvasGroup`。
- 删除节点时自动清理关联连线和成员不足的组合。
- 扩展 `tests/yCanvasDocument.test.mjs`，覆盖细粒度节点 patch、节点删除级联、连线 upsert 和组合 runtime patch。

##### 阶段 6C-2C：文本与历史增强（已完成）

- 将 `text.input.data.prompt` 升级为 `Y.Text`。
- 将图片生成 prompt 的编辑缓冲与 Y.Text 合并策略接好。
- 调整 `Y.UndoManager` tracked origins，让拖拽、文本编辑、运行状态有合理撤销粒度。
- 文本输入时实时写入 Y.Doc，blur 继续保留兼容更新路径。
- `snapshotFromYDoc` 导出时仍将 prompt 还原为普通字符串，兼容现有保存 API。
- 增加测试验证 prompt 存储为 `Y.Text`、细粒度文本 patch 保留同一个 Y.Text 实例、撤销/重做可恢复文本内容。
- React Flow 状态仍保留为 UI 派生层，后续可进一步减少双写风险。
- 保留现有 snapshot API，作为保存和导出兼容层。

验收标准：

```text
6C-2A 已完成：单人模式已具备 Y.Doc 文档桥接、快照转换、更新编码、撤销/重做入口和测试覆盖。
6C-2B 已完成：关键画布结构操作已写入 Y.Doc 细粒度 API，并有自动化测试覆盖。
6C-2C 已完成：prompt 已升级为 Y.Text，文本输入、快照导出和撤销/重做均已覆盖。
6C-2 整体完成标准：单人模式完全通过 Y.Doc 驱动画布，现有保存、恢复、撤销、重做、组合运行、节点缓存功能保持可用。
```

#### 阶段 6C-3：服务端 Yjs 增量同步（已完成 6C-3A / 6C-3B / 6C-3C）

开发内容：

##### 阶段 6C-3A：Yjs update relay 与前端接入（已完成）

- 后端安装 `yjs` 并在协作服务中维护每个 canvasId 的 Y.Doc room。
- 新增 `yjs:sync` 消息：客户端加入房间时，服务端发送当前 room 的 Y.Doc state update。
- 新增 `yjs:update` 消息：客户端发送 Y.Doc update，服务端应用到 room 文档并广播给其他客户端。
- 前端 collaboration client 支持 Yjs update base64 编解码、发送和接收。
- CanvasPage 观察本地 Y.Doc 变化并发送 update，收到远端 update 后应用到本地 Y.Doc。
- 保留旧 `snapshot:update` / `snapshot:request` 协议作为兼容兜底。
- 增加服务端 Yjs update 编码/应用测试。

##### 阶段 6C-3B：sync/state-vector 差量握手（已完成）

- 扩展后端协作服务，支持 `yjs:state-vector` 差量同步请求。
- 服务端 `yjs:sync` 根据客户端 state vector 只返回客户端缺失的 Y.Doc update。
- 服务端 `yjs:sync` 同时携带服务端 state vector，客户端据此回传服务端缺失的本地差量 update。
- 客户端进入画布和断线重连后自动发起 state vector 握手，避免只同步在线用户而画布为空。
- 保留本地 Y.Doc transaction update 节流广播，降低拖拽等高频操作的 WebSocket 压力。
- 增加服务端 state-vector 双向差量握手测试。

##### 阶段 6C-3C：awareness 兼容桥（已完成）

- 前端新增 awareness 兼容桥，将在线用户、光标、选中节点、editingNodeId 映射为 awareness local state。
- 协作客户端新增 `awareness:list` / `awareness:update` / `awareness:remove` 兼容消息，同时保留旧 `presence:*` 消息。
- 服务端协作 room 保存 awareness wire state，新用户加入时下发当前 awareness 列表，更新和离线时广播变化。
- 当前在线用户列表、用户光标和正在编辑节点 UI 保持不变，底层状态已具备迁移到标准 awareness protocol 的接口。
- 增加服务端 awareness wire state 清洗测试，避免异常 payload 破坏在线状态。

验收标准：

```text
6C-3A 已完成：双端可通过 WebSocket 交换 Yjs update，服务端维护 Y.Doc room，并保留 snapshot 兜底。
6C-3B 已完成：客户端和服务端通过 state vector 进行双向差量握手，新浏览器加入或重连时可合并 Y.Doc 内容，避免只显示用户信息而画布为空。
6C-3C 已完成：presence UI 已通过 awareness 兼容桥承载在线用户、光标、选中节点和编辑状态，后续可将传输层替换为标准 awareness protocol。
6C-3 整体完成标准：双浏览器同时编辑同一画布时，节点、连线、组合和文本编辑通过 Yjs 增量同步，不再依赖整份 snapshot 覆盖。
```

#### 阶段 6C-4：Yjs 持久化与历史基础（已完成）

开发内容：

- 新增服务端 Yjs 持久化服务，保存 `workflowUpdates` 增量日志。
- 支持将 update log 压缩为 `workflowSnapshots`，降低长期协作日志体积。
- 协作 room 首次加载时按 canvasId 从最新 Yjs snapshot + 后续 update replay 恢复 Y.Doc。
- 新增 `GET /api/canvases/:id/yjs-history`，为阶段 6D 历史版本页面准备 snapshot 列表接口。
- 新增 `POST /api/canvases/:id/yjs-compact`，支持手动压缩当前 room 的 Y.Doc。
- 保留现有 `CanvasSnapshot` 保存接口作为 JSON 导出/兼容层；后续 6D 中再补 UI 层历史回滚和导入导出。
- 增加自动化测试验证 Yjs snapshot + incremental update 可以恢复完整文档。

验收标准：

```text
6C-4 已完成：服务重启后能从本地 JSON 数据库恢复 Yjs 协作文档，并可列出/生成 Yjs 压缩快照，为 6D 历史版本页面打基础。
```

### 阶段 6D：项目管理与历史版本（已完成 6D-1 / 6D-2 / 6D-3）

开发内容：

- 项目列表页。
- 项目新建、切换、删除、复制。
- 画布快照历史。
- 查看历史版本。
- 回滚到历史版本。
- 导入 / 导出项目 JSON。

#### 阶段 6D-1：项目管理与导入导出入口（已完成）

开发内容：

- 顶栏接入项目选择器，支持项目切换、新建、重命名、复制和删除。
- 新增项目 JSON 导出接口，导出项目、画布、素材、Yjs update log 与 Yjs snapshot。
- 新增项目 JSON 导入接口，导入时生成新项目和新画布 ID，并重映射 Yjs 历史记录。
- 新增项目复制和删除后端服务，删除最后一个项目时自动创建兜底项目与主画布。
- 顶栏接入导入/导出按钮和 Yjs 历史入口。
- 新增 Yjs 历史快照面板，支持查看快照时间、clock、update 数量、update 大小，并可手动压缩当前文档。
- 增加项目归档服务测试，覆盖导入、复制、删除和 Yjs 历史重映射。

验收标准：

```text
6D-1 已完成：用户可在创作界面管理项目、导入/导出项目 JSON，并查看/生成当前画布的 Yjs 历史快照。
```

#### 阶段 6D-2：历史版本详情与回滚（已完成）

目标：把 6D-1 的快照列表升级为可检查、可恢复的历史版本能力。

开发内容：

- 增加历史快照详情接口，按 snapshotId 恢复 Y.Doc 并转换为 CanvasSnapshot 预览数据。
- 前端历史面板增加快照详情、节点/连线/组合数量摘要。
- 增加“恢复到此版本”操作，恢复前自动保存当前版本并生成回滚前快照。
- 恢复后同步写入 Y.Doc 和 CanvasSnapshot 兼容层，确保其他浏览器能收到增量更新。
- 增加回滚确认、失败提示和基础自动化测试。

验收标准：

```text
6D-2 已完成：用户可查看 Yjs 历史快照详情摘要，并在确认后恢复到选中历史版本；恢复前会自动保存当前版本，恢复后会同步当前画布与协作者。
```

#### 阶段 6D-3：项目列表页与历史体验打磨（已完成）

目标：把顶栏入口升级为更完整的项目管理体验，并提升历史恢复的可观察性。

开发内容：

- 增加项目列表/项目抽屉，集中展示项目、画布数量、更新时间和操作入口。
- 历史面板增加恢复前后差异提示、空快照保护和失败状态提示。
- 增加历史快照删除/清理策略的后端接口规划。
- 增加导入 JSON 的版本兼容校验与错误提示。

测试内容：

- 项目列表摘要能正确统计画布、素材和 Yjs 历史数量。
- 导入项目 JSON 时能拒绝不兼容版本、空画布列表和无法匹配 canvasId 的历史记录。
- 历史恢复前能展示当前/目标摘要，空快照会有额外保护。

阶段验收标准：

```text
6D-3 已完成：用户可以在项目抽屉中集中管理项目，历史恢复具备可观察的差异提示、空快照保护和失败提示，导入 JSON 前会进行基础兼容校验。
```

## 阶段 7：分端、模型运维与上线准备

目标：把客户使用的创作端和开发人员使用的模型配置后台彻底分清，避免客户前端暴露供应商、密钥、请求模板和调试信息；同时为内测和小规模上线补齐稳定性、权限和部署基础。

### 阶段 7A：客户创作端与开发者模型配置后台分离（进行中）

目标：将现有同一前端中的 `/admin` 模型管理能力迁出客户创作端，形成独立的开发者/运维后台。客户只看到可用模型和公开参数，不接触供应商配置、密钥、请求模板、内部任务诊断等敏感信息。

端边界：

- 客户创作端：面向最终客户使用，保留项目、画布、节点、素材、协作、历史、公开模型选择和公开参数调节。
- 开发者后台：面向开发人员和运维人员使用，管理供应商、模型、适配器、参数 schema、密钥、连接测试和任务诊断。
- 后端 API：按公开接口和内部接口分层，公开接口服务客户前端，内部接口服务开发者后台。

推荐目录与服务形态：

```text
apps/
  web/      客户创作端，不再包含 /admin 路由
  admin/    开发者模型配置后台，独立 Vite 应用，建议端口 5190
  api/      统一后端，提供 /api/* 公开接口和 /internal/* 内部接口
```

公开接口规划：

```text
GET /api/models
```

只返回客户前端可见的模型信息：

```typescript
interface PublicModelConfig {
  id: string;
  displayName: string;
  type: 'image' | 'audio' | 'video' | 'text';
  capabilities: string[];
  publicParamSchema: Record<string, unknown>;
  defaultPublicParams: Record<string, unknown>;
  enabled: boolean;
}
```

内部接口规划：

```text
GET    /internal/providers
POST   /internal/providers
PATCH  /internal/providers/:id
POST   /internal/providers/:id/test
GET    /internal/models
POST   /internal/models
PATCH  /internal/models/:id
GET    /internal/tasks
POST   /internal/tasks/:id/cancel
POST   /internal/tasks/:id/retry
```

内部接口可以返回和维护：

- providerId、baseUrl、authType、secretStorage。
- adapter、requestTemplate、pollingTemplate、resultMapping、errorMapping。
- timeoutSeconds、enabled、allowMockFallback。
- 连接测试结果、任务诊断、失败摘要。

安全边界：

- 客户前端不得调用 `/internal/*`。
- 客户前端不得接收 baseUrl、secretValue、adapter 请求模板、轮询模板、字段映射等内部配置。
- 开发者后台先支持 `INTERNAL_ADMIN_TOKEN` 或本机/内网限制；后续再接正式权限系统。
- API 日志不得打印密钥、Authorization header、完整请求模板中的敏感字段。
- 现阶段 `plain-local-json` 仍可作为开发环境兼容方案，但需要为后续加密落库保留服务抽象。

开发内容：

- 新建 `apps/admin`，迁移现有 `AdminPage` 相关模型供应商、模型列表、任务诊断 UI。（已完成）
- 客户端 `apps/web` 移除 `/admin` 路由和开发者配置入口。（已完成）
- 后端新增公开模型接口 `/api/models`，只返回脱敏后的公开模型信息。（已完成）
- 后端将现有 `/api/admin/*` 迁移或兼容映射到 `/internal/*`。（已完成）
- 增加内部接口访问保护：优先实现 `INTERNAL_ADMIN_TOKEN`，并支持本地开发模式免 token。（已完成）
- 前端画布节点改用 `/api/models` 获取可选模型。（已完成）
- 客户端 `/admin` 路径显示不可用入口，不渲染画布或开发者后台。（已完成）
- 开发者后台模型表单分离内部 `defaultParams` / `paramSchema` 与客户公开 `defaultPublicParams` / `publicParamSchema`。（已完成）
- 后端为历史模型补齐公开参数字段，`/api/models` 优先返回公开参数。（已完成）
- 文档中明确客户端和开发者后台的启动地址、部署边界和敏感信息边界。（已完成）

测试内容：

- 客户端访问不到 `/admin` 页面。
- 客户端 `GET /api/models` 不返回 baseUrl、secretValue、adapter、requestTemplate 等内部字段。
- 客户端 `GET /api/models` 只消费 `defaultPublicParams` 和 `publicParamSchema`。
- 开发者后台可以通过 `/internal/*` 管理供应商和模型。
- 未授权请求 `/internal/*` 会被拒绝。
- 授权开发者请求 `/internal/*` 可正常读写模型配置。
- 现有图片生成链路继续使用内部模型配置执行任务。
- 管理端任务取消、重试、测试连接功能迁移后仍可用。

阶段验收标准：

```text
7A 完成后，客户创作端和开发者模型配置后台在路由、构建、接口和数据可见性上明确分离；客户只能选择公开模型和公开参数，开发人员通过独立后台维护供应商、密钥、适配器和任务诊断。
```

### 阶段 7B：模型参数可视化与自定义 HTTP 适配器增强（已完成）

目标：在 7A 分端完成后，继续强化开发者后台的模型配置能力，并让客户创作端只消费公开参数 schema。

开发内容：

- 开发者后台支持用 `paramSchema` 可视化编辑模型默认参数。（已完成）
- 客户节点侧根据 `publicParamSchema` 渲染公开参数表单。（已完成）
- 自定义 HTTP 适配器支持请求方法、路径、请求体模板、轮询模板、任务 ID 路径、状态路径、成功/失败状态值、结果字段映射和错误字段映射。（已完成）
- 供应商测试连接返回基础连通、鉴权状态、延迟和错误摘要。（已完成）
- 任务详情增加输入摘要、模型、供应商、耗时、失败原因和重试入口。（已完成：后台任务列表展示模型名称、供应商、耗时、错误分类标签；点击任务弹出详情面板，展示完整输入摘要、参数、耗时、错误分类、原始错误信息和重试入口）

测试内容：

- 不手写 JSON 也能编辑模型参数。（已验证）
- 客户节点能选择模型并覆盖公开参数。（已验证：客户侧只显示尺寸、宽高比两个公开参数）
- 新自定义 HTTP 图片模型能通过后台配置接入。（已验证）
- 模型不可用时能区分 URL、鉴权、超时和字段映射错误。（已验证：connection/auth/timeout/non_json/field_mapping/model_error 六类错误分类）

阶段验收标准：

```text
7B 已完成：开发人员可以在独立后台配置和诊断模型服务；客户创作端只消费公开模型和公开参数，能稳定发起生成任务。
```

### 阶段 7C：内测稳定性、权限与上线准备

开发内容：

- 增加权限系统。
- 增加团队空间。
- 增加项目分享。
- 增加画布性能优化。
- 增加任务统计。
- 增加错误监控。
- 增加部署脚本。
- 增加自动化测试和端到端验收。
- 拆分 `CanvasPage.tsx` 中的项目抽屉、历史面板、素材库、节点卡片和协作状态。（7C-1A 已完成首轮低风险拆分：节点卡片、连线、协作/组合覆盖层、参数控件、快照/ReactFlow 转换工具已迁出，主页面从 3227 行降到约 2031 行）
- 增加 `data/db.json` 备份/恢复策略，导入和历史恢复前自动备份。（7C-1B 已完成：新增 `data/backups` 本地备份，项目导入和历史恢复写入前必须备份成功，备份失败返回 503 并阻断写入）
- 7C-1B：实现 `data/db.json` 本地备份策略，项目导入和历史恢复前自动备份；备份失败时阻断危险写入。（已完成）
- 7C-1C：增加大画布基础性能基准，覆盖 100/300/500 节点加载、选择、拖拽和保存。（已完成：新增 `npm run benchmark:canvas`，首轮 500 节点基线为加载当前路径 3.14ms、预索引参考路径 1.94ms、选择 0.45ms、拖拽 0.62ms、保存 0.36ms）
- 7C-1D：优化大画布加载转换路径，`toReactFlowNodes` 改为一次性构建节点/入边索引，减少每个节点重复扫描。（已完成：500 节点旧扫描对照 3.33ms，优化路径 1.90ms；基准脚本加入旧路径/索引路径输出一致性断言）
- 7C-1E：优化大画布拖拽停止后的组合边界重算路径，`recomputeGroups` 改为一次性构建节点索引后按组成员取节点。（已完成：500 节点旧拖拽重算 0.62ms，优化路径 0.06ms；基准脚本加入旧/新拖拽输出一致性断言）
- 7C-1F：优化大画布选择/批量操作路径，选中节点读取、框选建组、拖拽后选区边界和多选删除改为节点索引/集合/批量删除路径。（已完成：500 节点旧选择 0.44ms，优化选择 0.07ms；旧循环删除 1.06ms，批量删除 0.05ms；基准脚本加入旧/新选择和删除输出一致性断言）
- 7C-1G：优化大画布保存/快照序列化路径，缓存当前画布的 `CanvasSnapshot` 与 JSON，历史比较、localStorage、自动保存和手动保存复用同一份序列化结果，并新增 `saveSnapshotJson` 避免请求阶段二次 stringify。（已完成：500 节点旧重复保存序列化 1.15ms，缓存路径 0.37ms；基准脚本加入旧/新保存输出一致性断言）
- 7C-1H：优化协作广播和自动保存调度路径，增加本地快照、待广播快照、已广播快照、待保存快照和已保存快照去重，手动保存会取消等待中的自动保存并复用同一份 JSON body。（已完成：500 节点连续编辑调度操作从 192 降到 48；自动保存改为 `pendingSaveRef` 驱动，避免旧 timer/stale closure 覆盖新快照状态）
- 7C-1I：优化大画布渲染路径，节点卡片改为 `memo` 自定义比较，未展开节点忽略素材/模型数组刷新，展开态素材筛选延迟计算，并启用 React Flow `onlyRenderVisibleElements`。（已完成：500 节点素材库刷新触发的节点渲染数从 500 降到 1；基准脚本加入素材刷新渲染数对照）
- 7C-1J：优化生产包体拆分，Web Vite 构建按 React、React Flow、Yjs、WeUI 分离稳定 vendor chunk，移除 500kB 主包警告。（已完成：Web 主业务 JS 从约 504kB 降到约 90kB，最大 JS chunk 为 React Flow vendor 约 181kB，构建无循环 chunk 警告）
- 7C-1K：新增预发/内测一键检查脚本，串联 Node/依赖检查、本地 API/Web/Admin 探活、画布性能基准和完整 `npm run check`。（已完成：新增 `npm run preflight`；修复 PowerShell 原生命令退出码捕获，避免 check 失败被误判通过；预发脚本已全绿）
- 7C-1L：补齐发布前运维说明，集中记录本地启动、预检流程、客户/开发者入口隔离、环境变量、数据备份、常见故障和发布前 checklist。（已完成：新增 `发布前运维说明.md`，并在 README 与使用说明中加入预检入口）
- 7C-1M：增加内部接口权限与发布前安全检查，验证 `/api/models` 脱敏、`/internal/*` 与兼容 `/api/admin/*` 的 token 行为，以及客户创作端源码/构建产物不包含内部入口或 token 引用。（已完成：新增 `npm run security:preflight` 和严格模式 `npm run security:release`，并接入 `npm run preflight`）
- 7C-1N：错误监控与运行日志收口，后端记录 API 启动、安全拦截、任务失败、模型配置变更、供应商测试异常、备份失败等运行事件，并在开发者后台新增“运行日志”页。（已完成并验证：新增 `systemEvents` 环形记录、`GET /internal/system-events`、运行日志筛选和摘要卡片；事件元数据自动脱敏 token/secret/authorization/apiKey；`npm run check` 通过，内置浏览器已验证 Admin 运行日志页和客户创作端隔离，临时 token 环境已验证未授权 internal 访问会记录 `security/warning` 事件）
- 7C-1O：发布候选端到端验收固化，将上一轮手动验证的 API/Web/Admin、公开模型契约、内部接口、任务富化、运行日志筛选、客户 `/admin` 隔离和 token 安全日志分支沉淀为可重复冒烟脚本。（已完成：新增 `npm run smoke:release` 与 `scripts/release-smoke.ps1`，并接入 `npm run preflight`；脚本使用独立临时数据目录和随机端口验证未授权 internal 访问会记录 `security/warning` 事件）
- 7C-1P：内测数据与恢复演练，将项目导入、历史恢复、备份失败阻断和手动恢复 `db.json` 的流程固化为隔离演练脚本。（已完成：新增 `npm run recovery:drill` 与 `scripts/recovery-drill.ps1`，并接入 `npm run preflight`；脚本使用临时 `DATA_DIR` 和随机端口 API 验证备份路径不可写时 503 阻断写入并记录 `backup/error`，正常导入和历史恢复会生成可恢复备份，拷回历史恢复备份后重启 API 可读回旧快照）
- 7C-1Q：素材库刷新状态对账，修复浏览器刷新后当前项目后端素材数正常但 footer/素材抽屉显示 0 的状态问题，并修复素材库抽屉前端布局溢出。（已完成：项目列表按最近活动排序，前端素材状态绑定 projectId 并使用请求序号阻断 stale 响应覆盖，footer 在素材列表加载前回退使用项目摘要 `assetCount`，素材抽屉显示加载态；素材抽屉 CSS 增加作用域，避免被旧 `.asset-grid` 全局规则污染，改为单列紧凑卡片、无横向滚动、操作按钮稳定排布；新增 `assetRuntime` 单元测试和项目列表排序测试，内置浏览器已验证素材 30 条时抽屉布局正常）
- 7C-1R：连接空白处快速补节点，拖拽输入/输出端口连线并在空白画布松开时弹出兼容节点选择浮窗，选择节点后自动创建并连接。（已完成：新增 `connectionPicker` 规则层，候选节点复用 `validateConnection` 校验方向、媒体类型、重复连线、端口多连和环路；输出端口只推荐可接受当前输出媒体的下游生成节点，例如图片输出可选择图片生成/视频生成且不推荐音频生成；输入端口只推荐可满足当前输入的上游输入节点；CanvasPage 接入 `onConnectStart` / `onConnectEnd`、浮窗定位、空候选提示、自动建节点与 Yjs/React Flow 边写入；新增 `connectionPicker` 单元测试并通过，Web 构建通过）
- 7C-1S：后端断线恢复与连线释放兜底，修复 `db.json` 截断损坏导致 API 反复断开，以及连线释放在真实空白画布上不弹候选浮窗的问题。（已完成：后端读取统一走 `readDbWithRecovery`，启动时可从 `db.json.tmp` / 备份候选恢复并保留损坏文件，写入路径保留临时文件校验和 rename 重试且不再直接覆盖主库；任务队列本地开发默认走本进程 local 队列，只有显式 `TASK_QUEUE_MODE=bullmq` 才连接 Redis/BullMQ，避免无 Redis 环境下 API 启动后反复掉线；本地启动脚本改为直接运行 API 服务进程，避免 `node --watch` 父进程残留但 8787 服务子进程退出导致健康检查误判；前端初始化失败后进入本地模式并自动重试后端连接，恢复后重新加载用户、项目、画布、模型和素材；CanvasPage 新增全局 pointer/mouse/touch 释放兜底，React Flow 释放回调和兜底共用同一套候选生成逻辑，并短暂屏蔽 pane click 防止浮窗刚打开就被关闭；已确认当前 `data/db.json` 可解析且包含项目/画布/素材/任务数据，Web 构建通过）
- 7C-1T：PostgreSQL 部署与用户数据隔离设计，形成服务器部署计划并完成服务器端 Docker PostgreSQL 16 部署验收。（已完成：服务器 `/ssd_data/PostgreSQL/postgres-service` 已启动 `anime-canvas-postgres`，`check.sh` 确认 `anime_canvas` 数据库、`anime_canvas_app` 用户和 `users/user_devices/projects/project_members/canvases/assets/tasks/providers/models/system_events/yjs_updates/yjs_snapshots` 12 张表存在；部署文档已补充“后端程序通过局域网连接服务器 PostgreSQL，前端浏览器不直连数据库”的端口策略）
- 7C-1U：PostgreSQL 存储适配层代码接入，保留 JSON fallback，先以兼容当前业务的整库快照接口接通 PostgreSQL。（已完成首版：新增 `DATA_BACKEND=postgres` 后端开关、`pg` 驱动依赖、`postgresStore` 适配层、PostgreSQL 表到当前 JS 快照对象的双向映射、事务内整库替换写入、跨进程 advisory lock、默认数据/历史模型字段规范化复用、`npm run postgres:dry-run` 迁移前对账脚本、`npm run postgres:smoke` 局域网数据库连通性检查，以及映射层单元测试）
- 7C-1V：JSON 到 PostgreSQL 迁移脚本与导入后验收。（已完成：服务器 PostgreSQL 位于 `192.168.100.100:5432`，`anime_canvas_app` 通过局域网连接成功；`postgres:migrate --apply --confirm replace-postgres --no-strict` 正式导入 7 users / 2 project_members / 2 projects / 4 canvases / 57 assets / 110 tasks / 4 providers / 3 models / 164 system_events / 49 yjs_updates / 13 yjs_snapshots，数量对账通过；`postgres:api-smoke` 验证 API 在 `DATA_BACKEND=postgres` 下 health/models/projects/canvases/assets/admin-overview 均正常；迁移过程中修复了 `system_events.level` CHECK 约束不兼容 `warning` 的问题——在 `postgresStore` 迁移层做 `warning→warn` 映射，`systemEventService` 同时接受 `warn` 和 `warning`；`postgres-migration-utils` 新增 `adjustedExpectedCounts` 处理因引用缺失项目而被跳过的 asset，确保对账准确）

测试内容：

- 权限隔离正确。
- 非项目成员不能访问项目。
- 大量节点下画布仍可操作。
- 高并发任务不会导致队列异常。
- 后端异常时前端有明确提示。
- 部署后服务可稳定运行。

阶段验收标准：

```text
系统达到可内测或小规模上线状态。
```

---

## 10. 后续升级路线

## 10.1 画布引擎升级路线

现阶段：

```text
React Flow / XYFlow
```

适合快速开发 MVP。

后续选择：

| 路线 | 适用场景 | 说明 |
| --- | --- | --- |
| 继续深度定制 React Flow | 节点数量中等，业务优先 | 维护成本低，迭代快 |
| DOM + Canvas 混合渲染 | 连线很多、节点 UI 复杂 | 节点用 DOM，连线用 Canvas |
| WebGL 画布引擎 | 上千节点、复杂动效 | 性能强，但开发成本高 |
| 自研画布引擎 | 产品交互高度特殊 | 控制力最强，但周期最长 |

推荐升级顺序：

```text
React Flow → DOM + Canvas 混合 → WebGL / 自研引擎
```

## 10.2 协作系统升级路线

现阶段：

```text
Yjs + WebSocket + awareness + 定时快照
```

后续升级：

- 增加服务端 Yjs 文档持久化。
- 增加操作日志。
- 增加版本历史。
- 增加历史回滚。
- 增加硬锁。
- 增加评论和批注。
- 增加团队权限。
- 增加多人语音/会议协作入口。

## 10.3 AI 模型系统升级路线

现阶段：

```text
后台手动配置模型供应商和模型参数
```

后续升级：

- 支持多供应商自动路由。
- 支持失败自动切换备用模型。
- 支持按成本、速度、质量选择模型。
- 支持模型调用统计和成本统计。
- 支持用户级模型权限。
- 支持团队级模型额度。
- 支持插件化模型适配器。
- 支持 ComfyUI 工作流模板导入。
- 支持用户自定义模型节点。

## 10.4 节点系统升级路线

现阶段：

```text
内置固定节点
```

后续升级：

- 插件化节点注册。
- 自定义节点参数表单。
- 节点模板市场。
- 工作流模板市场。
- 动漫角色一致性节点。
- 分镜脚本节点。
- 字幕节点。
- 导出成片节点。
- 批量生成节点。

## 10.5 后端架构升级路线

现阶段：

```text
单体 API + Worker + Redis + PostgreSQL + 对象存储
```

后续升级：

- API 服务拆分。
- Worker 横向扩容。
- 视频任务独立服务。
- 文件转码独立服务。
- 任务事件总线。
- 多租户隔离。
- Kubernetes 部署。
- CDN 加速素材访问。

---

## 11. 推荐优先级

第一优先级：

```text
画布 MVP + 文本节点 + 图片节点 + 图片生成节点 + 图片生成链路
```

第二优先级：

```text
项目保存 + 素材库 + 模型后台配置
```

第三优先级：

```text
Yjs + WebSocket 多人协作
```

第四优先级：

```text
音频生成 + 视频生成
```

第五优先级：

```text
工作流执行引擎 + 插件化节点 + 性能优化
```

---

## 12. 最小可用版本 MVP 定义

MVP 只做最小闭环：

```text
创建项目
  ↓
进入无限画布
  ↓
添加文本节点
  ↓
输入动漫提示词
  ↓
添加图片生成节点
  ↓
连接文本节点和图片生成节点
  ↓
点击生成
  ↓
后端调用图片模型
  ↓
生成图片
  ↓
图片显示在节点中
  ↓
生成结果保存到素材库
```

MVP 不做：

- 完整多人协作。
- 视频生成。
- 音频生成。
- 硬锁。
- 插件市场。
- 复杂权限。
- 自研画布引擎。
- 工作流全自动运行。

---

## 13. 开发建议总结

建议先用成熟方案快速跑通产品核心闭环。

现阶段推荐路线：

```text
React + TypeScript + WeiUI + React Flow
Node.js + PostgreSQL + Redis + BullMQ
Yjs + WebSocket
MinIO / S3
后台模型管理 WebUI
```

先完成单人可用的 AI 图片生成工作流，再接入模型后台和素材库，然后再做 Yjs 多人协作。这样风险最低，开发节奏最稳。

后续再逐步扩展音频、视频、工作流自动运行、硬锁、模板市场、插件系统和 WebGL 级别画布性能优化。
