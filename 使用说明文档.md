# 无限画布 AI 动漫创作工具使用说明

## 1. 环境要求

- Node.js 20 或更高版本
- npm
- PowerShell
- 可选：本地 Z-Image Turbo 服务，默认地址为 `http://localhost:8192`

## 2. 首次安装

在项目根目录执行：

```powershell
cd E:\画板
npm run setup
```

该命令会安装前端和后端依赖。

## 3. 启动服务

启动前端和后端：

```powershell
npm run dev
```

启动后访问：

- 创作端：`http://localhost:5180`
- 管理端：`http://localhost:5180/admin`
- API 健康检查：`http://localhost:8787/api/health`

停止服务：

```powershell
npm run stop
```

也可以分别启动：

```powershell
npm run dev:api
npm run dev:web
```

## 4. 基础创作流程

1. 打开 `http://localhost:5180`。
2. 从左侧节点库拖入“文本节点”。
3. 选中文本节点，在右侧属性面板输入提示词。
4. 拖入“图片生成”节点。
5. 从文本节点的输出端口连接到图片生成节点的提示词输入端口。
6. 点击图片生成节点上的“生成”按钮。
7. 系统会创建 AI 任务，前端轮询任务状态。
8. 任务成功后，图片会回写到节点并进入素材库。

## 5. 节点说明

当前节点包括：

| 节点 | 用途 | 当前状态 |
| --- | --- | --- |
| 文本节点 | 输入提示词文本 | 可用 |
| 图片节点 | 上传或选择图片素材 | 可用 |
| 音频节点 | 上传或选择音频素材 | 输入可用 |
| 视频节点 | 上传或选择视频素材 | 输入可用 |
| 图片生成 | 根据文本和参考图生成图片 | 可用 |
| 音频生成 | 后续扩展 | 占位 |
| 视频生成 | 后续扩展 | 占位 |

## 6. 连接规则

系统会自动校验节点连接：

- 只能从输出端口连接到输入端口。
- 媒体类型必须一致，例如 text 只能连接 text 输入。
- 必填输入未连接时不能运行。
- 非多输入端口只能连接一个来源。
- 禁止节点连接自身。
- 禁止形成循环依赖。

## 7. 组合运行

可以按住 Ctrl 框选多个节点，然后点击“组合选区”。

组合运行时，系统会基于连线构建 DAG，并使用拓扑排序决定生成节点执行顺序，确保上游节点先于下游节点运行。

如果组合内存在循环依赖，系统会阻止运行。

## 8. 管理端使用

访问：

```text
http://localhost:5180/admin
```

管理端当前支持：

- 查看模型供应商。
- 保存 Z-Image Turbo 供应商配置。
- 查看模型列表。
- 保存图片模型配置。
- 查看最近 AI 任务状态。

供应商密钥字段当前使用 `secretValue` 本地保存，并标记 `secretStorage=plain-local-json`。这表示开发环境明文 JSON 存储，不代表已经加密。接口返回时不会回传密钥原文，只返回 `hasSecret`。

## 9. AI 任务模式

当前 AI 任务已经升级为异步语义：

1. 前端调用 `POST /api/ai/tasks` 创建任务。
2. 后端立即返回 `202 Accepted` 和任务 ID。
3. 后端在本进程后台执行任务。
4. 前端轮询 `GET /api/ai/tasks/:taskId` 获取状态。
5. 任务成功后输出图片 URL，并创建素材记录。

任务状态包括：

- `pending`
- `running`
- `succeeded`
- `failed`

## 10. 本地数据

本地数据默认保存在：

```text
data/db.json
```

包含：

- projects
- canvases
- assets
- tasks
- providers
- models

## 11. 配置文件

参考 `.env.example`：

```text
API_PORT=8787
WEB_PORT=5180
DATA_DIR=data
VITE_API_BASE_URL=http://localhost:8787/api
Z_IMAGE_BASE_URL=http://localhost:8192
```

如需覆盖配置，可以创建 `.env` 文件。

## 12. 检查与测试

运行完整检查：

```powershell
npm run check
```

运行测试：

```powershell
npm test
```

当前测试覆盖：

- 工作流连接校验。
- 节点运行前置校验。
- 组合 DAG 拓扑排序。
- 供应商密钥返回脱敏。

## 13. 常见问题

### 生成失败提示模型服务不可用

请确认本地 Z-Image Turbo 服务是否启动，并检查 `.env` 中的 `Z_IMAGE_BASE_URL`。

### 任务一直 running

可能是模型服务没有返回 `finished` 或 `failed` 状态。可以进入管理端查看任务状态，并检查 API 日志。

### 修改后页面没有更新

如果使用 `npm run dev`，前端服务由脚本后台启动。开发时建议分别执行：

```powershell
npm run dev:api
npm run dev:web
```

这样可以直接看到前端热更新日志。
