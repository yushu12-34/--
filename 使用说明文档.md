# 无限画布 AI 动漫创作工具使用说明

## 1. 环境要求

- Node.js 20 或更高版本
- npm
- PowerShell
- 可选：本地 Z-Image Turbo 服务，默认地址为 `http://localhost:8192`

## 2. 首次安装

在项目根目录执行：

```powershell
cd E:\画板
npm run setup
```

该命令会安装前端和后端依赖。

## 3. 启动服务

启动前端和后端：

```powershell
npm run dev
```

启动后访问：

- 创作端：`http://localhost:5180`
- 开发者管理端：`http://localhost:5190`
- API 健康检查：`http://localhost:8787/api/health`

停止服务：

```powershell
npm run stop
```

也可以分别启动：

```powershell
npm run dev:api
npm run dev:web
```

## 4. 基础创作流程

1. 打开 `http://localhost:5180`。
2. 从左侧节点库拖入“文本节点”。
3. 选中文本节点，在右侧属性面板输入提示词。
4. 拖入“图片生成”节点。
5. 从文本节点的输出端口连接到图片生成节点的提示词输入端口。
6. 点击图片生成节点上的“生成”按钮。
7. 系统会创建 AI 任务，前端轮询任务状态。
8. 任务成功后，图片会回写到节点并进入素材库。

## 5. 节点说明

当前节点包括：

| 节点 | 用途 | 当前状态 |
| --- | --- | --- |
| 文本节点 | 输入提示词文本 | 可用 |
| 图片节点 | 上传或选择图片素材 | 可用 |
| 音频节点 | 上传或选择音频素材 | 输入可用 |
| 视频节点 | 上传或选择视频素材 | 输入可用 |
| 图片生成 | 根据文本和参考图生成图片 | 可用 |
| 音频生成 | 后续扩展 | 占位 |
| 视频生成 | 后续扩展 | 占位 |

## 6. 连接规则

系统会自动校验节点连接：

- 只能从输出端口连接到输入端口。
- 媒体类型必须一致，例如 text 只能连接 text 输入。
- 必填输入未连接时不能运行。
- 非多输入端口只能连接一个来源。
- 禁止节点连接自身。
- 禁止形成循环依赖。

## 7. 组合运行

可以按住 Ctrl 框选多个节点。框选完成后，系统会自动为该批节点生成组合背景，不再需要额外点击“组合选区”确认。

组合运行时，系统会基于连线构建 DAG，并使用拓扑排序决定生成节点执行顺序，确保上游节点先于下游节点运行。

如果组合内存在循环依赖，系统会阻止运行。

组合背景上的操作包括：

- 运行组合工作流。
- 重命名组合。
- 解除组合。

运行组合时，输入节点不会被执行，只作为上游数据源；生成节点会按依赖顺序执行。若某个上游生成节点失败，下游依赖节点会跳过，其他无依赖分支仍会继续运行。

## 8. 节点缓存与结果状态

生成节点会根据上游输入、节点 prompt、参考图、模型 ID 和参数计算输入签名。

- 输入未变化且已有结果时，点击“使用缓存运行”会直接命中缓存。
- 点击“强制重新生成”会忽略缓存并重新创建 AI 任务。
- 修改 prompt、参考素材、模型或参数后，节点会显示“结果可能已过期”。
- 组合运行时也会自动复用未变化节点的缓存结果。

## 9. 项目与历史管理

创作端顶部的“项目”按钮会打开项目抽屉，集中展示项目列表、画布数量、素材数量、历史快照数量和最近更新时间。

项目抽屉支持：

- 新建项目。
- 切换项目。
- 复制项目。
- 删除项目。
- 导出项目 JSON。
- 导入项目 JSON。

导入项目 JSON 时，系统会校验项目文件版本、画布结构、素材数组和 Yjs 历史记录引用关系；不兼容文件会直接提示失败，不会写入当前项目。

顶部“操作”菜单中的“历史”会打开当前画布的 Yjs 历史快照面板。

历史面板支持：

- 刷新历史快照列表。
- 手动压缩当前 Yjs 文档生成快照。
- 查看历史版本的节点、连线、组合摘要。
- 对比当前画布和选中历史版本的基础数量差异。
- 恢复到选中历史版本。

恢复历史版本前，系统会自动压缩保存当前版本。若目标历史版本为空画布，会进行额外确认；恢复失败时会在历史面板中显示错误信息。

## 10. 开发者管理端使用

访问：

```text
http://localhost:5190
```

当前开发者管理端已迁出为 `apps/admin` 独立应用，不面向客户开放。客户创作端不再提供 `/admin` 入口；开发者管理端改用 `/internal/*` 内部接口。

开发者管理端当前支持：

- 查看模型供应商。
- 保存 Z-Image Turbo 供应商配置。
- 查看模型列表。
- 保存图片模型配置，并分别维护内部执行参数和客户公开参数。
- 查看最近 AI 任务状态。
- 取消和重试后台任务。

供应商密钥字段当前使用 `secretValue` 本地保存，并标记 `secretStorage=plain-local-json`。这表示开发环境明文 JSON 存储，不代表已经加密。接口返回时不会回传密钥原文，只返回 `hasSecret`。

阶段 7A 的目标边界：

- 客户创作端只显示公开模型名称、能力和公开参数。
- 客户创作端命中 `/admin` 时只显示不可用入口，不进入开发者后台。
- 客户创作端不显示供应商、baseUrl、密钥、adapter、请求模板、轮询模板和任务内部诊断。
- 开发者管理端可独立维护 `defaultParams` / `paramSchema` 与客户可见的 `defaultPublicParams` / `publicParamSchema`。
- 开发者管理端独立部署或只在内网/本机开放。
- 后端公开接口使用 `/api/*`，内部模型配置接口使用 `/internal/*`。
- 如配置 `INTERNAL_ADMIN_TOKEN`，开发者管理端需要同时配置 `VITE_INTERNAL_ADMIN_TOKEN`。

## 11. AI 任务模式

当前 AI 任务已经升级为异步语义：

1. 前端调用 `POST /api/ai/tasks` 创建任务。
2. 后端立即返回 `202 Accepted` 和任务 ID。
3. 后端在本进程后台执行任务。
4. 前端轮询 `GET /api/ai/tasks/:taskId` 获取状态。
5. 任务成功后输出图片 URL，并创建素材记录。

任务状态包括：

- `pending`
- `running`
- `succeeded`
- `failed`
- `cancelled`

当前真实生成链路主要支持图片任务。音频/视频输入节点、占位生成节点和素材预览已经具备，但真实音频/视频模型适配仍待开发。

## 12. 本地数据

本地数据默认保存在：

```text
data/db.json
```

包含：

- projects
- canvases
- assets
- tasks
- providers
- models
- users

## 13. 配置文件

参考 `.env.example`：

```text
API_PORT=8787
WEB_PORT=5180
DATA_DIR=data
VITE_API_BASE_URL=http://localhost:8787/api
VITE_INTERNAL_API_BASE_URL=http://localhost:8787/internal
INTERNAL_ADMIN_TOKEN=
VITE_INTERNAL_ADMIN_TOKEN=
Z_IMAGE_BASE_URL=http://localhost:8192
Z_IMAGE_REMOTE_BASE_URL=http://192.168.100.100:8191
```

如需覆盖配置，可以创建 `.env` 文件。

可选配置：

- `REDIS_URL`：启用 BullMQ 队列。
- `TASK_QUEUE_MODE=local`：强制使用本进程本地队列。
- `MINIO_ENDPOINT` / `MINIO_PORT` / `MINIO_BUCKET`：启用对象存储。
- `VITE_COLLAB_WS_URL`：覆盖协作 WebSocket 地址。

## 14. 检查与测试

运行完整检查：

```powershell
npm run check
```

运行测试：

```powershell
npm test
```

当前测试覆盖：

- 工作流连接校验。
- 节点运行前置校验。
- 组合 DAG 拓扑排序。
- 供应商密钥返回脱敏。
- Yjs update 同步、state-vector 差量握手、awareness 状态清洗。
- Yjs 持久化、历史快照详情、项目导入/复制/删除和导入校验。
- Y.Doc 画布转换、细粒度节点/连线/组合操作、prompt 撤销/重做。

## 15. 常见问题

### 生成失败提示模型服务不可用

请确认本地 Z-Image Turbo 服务是否启动，并检查 `.env` 中的 `Z_IMAGE_BASE_URL`。

### 任务一直 running

可能是模型服务没有返回 `finished` 或 `failed` 状态。开发者可以进入开发者管理端查看任务状态，并检查 API 日志。

### 修改后页面没有更新

如果使用 `npm run dev`，前端服务由脚本后台启动。开发时建议分别执行：

```powershell
npm run dev:api
npm run dev:web
```

这样可以直接看到前端热更新日志。
